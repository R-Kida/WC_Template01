# taisei-site エリア・ブロック登録チェックリスト

WebChanger化ツール「6. エリア・ブロックの指定」作業を効率化するための実務用
チェックリスト。**設計判断の理由や代替案は載せていない**（それは
`taisei-site-area-block-guide.md`の役目）。ここは「どこを探して、
何に登録するか」だけを一直線に並べた、作業しながら上から潰していくための
リスト。

## 使い方

1. 「検索文字列」を化ツールのHTMLソース表示内で検索（Ctrl+F等）し、該当箇所へ
   ジャンプする。
2. 「登録先」の通りに登録する。
3. **`【共通・1回だけ】`の付いた項目は、4テンプレートのうち1ページ（どれでもよい。
   おすすめは`toppage.html`）で1回登録すれば、共通エリア（`cmn`）として
   全ページに反映される。** 同じ検索文字列は他の3ページにも存在するが、
   探しに行く必要はない。
4. `【ページ固有】`の項目は、そのページでしか使わないので、該当ページを開いた
   ときにだけ登録する。
5. チェックボックスはそのまま実施記録として使ってよい。
6. **✅2026-08-19・うにさんの指摘**：ある項目の登録が終わり、対応する`[WC]`
   コメントを読み終えたら、そのコメントは静的HTML側から削除してよい（むしろ
   削除推奨）。理由：`[WC]`コメントがエリア直下に残っていると「エリア直下は
   全てブロック要素で構成されていなければならない」バリデーションエラーの
   原因になる（CLAUDE.md 3節参照）。エリアの外側に置く配置ルールを守っていても、
   後続の構造変更で意図せずエリア内に入り込むリスクがあるため、役目を終えた
   コメントはその都度消してしまうのが一番安全。最終的に変換完了後は全コメントを
   削除する想定（2節参照）なので、登録を進めながら少しずつ削除していく形でよい。

---

## A. 全ページ共通・1回だけ登録するもの

### A-1. ロゴ
- [ ] 検索: `id="logo"` → 共通エリア`cmn001`相当として登録
  - [ ] 検索: `class="logoIcon"` → 画像ブロック1つ（`<img class="logoMark">`）
  - [ ] 検索: `class="logoText"` → テキストブロック1つ（`<div class="logoText">`。
        リンク先〈トップページ〉が必要な場合は、静的HTMLにhrefを持たせず、
        WebChangerのブロックリンク機能〈rdhaslink〉で登録後に付与する）
- **✅2026-08-19・実機エラーを3回受けて修正済み（詳細はtoppage.htmlの`#logo`
  直前の`[WC]`コメント参照）**：
  1. 当初`<a>`の中にsvgアイコン＋テキストが混在しており「親エリアに指定できない」
     エラーが発生（navCaretと同じ原因）→ アイコンを`.logoIcon`（固定div、`<a>`の外）
     に分離済み。
  2. `#logo`をエリア登録した後、`.logoIcon`をブロック化しようとしたところ、中身が
     生のsvgのため「編集ブロック配下のコンテンツ枠がブロック要素でない」エラーが
     発生（`.imgPlaceholder`と同じ原因）→ svgをdata URI化した`<img class="logoMark">`
     に置き換え済み。
  3. `<a class="logoLink"><span class="logoText">...</span></a>`を右クリック→
     「編集ブロック化」で明示的にテキストブロック登録しようとしたが、これも
     「1要素として認識されない」エラーが再発（うにさんの指摘：divで囲われていないと
     1要素にならない）→ `<a>`ごと廃止し`<div class="logoText">`に置き換えて解消済み。
  - フッターの`.footerBrand`側のロゴ（A-4参照）も同じ構造・同じ対応済み。

### A-2. ページ最上部（営業時間・案内リンク）
- [ ] 検索: `id="utilityBar"` → 共通エリア `cmn002` 相当
- 配下は個別に3ブロック（同じエリアの直下にそれぞれ`<div>`で包んである）：
  - [ ] 検索: `営業時間 11:00` → テキストブロック（ページ内で一意）
  - [ ] 検索: `初めての方へ` → **ページ内に3箇所ヒットする（1件目=ここ、2件目=
        ナビ、3件目=フッター）ので、一番最初のヒットを使う** → テキストブロック
  - [ ] 検索: `個人情報保護方針` → テキストブロック（ページ内で一意）

### A-3. メインナビ（ドロップダウン込み）
- [ ] 検索: `id="globalNav"` の直下の `class="navUl"` → 共通エリア（`useronly="1"`）
- ページ内の他の箇所（`#features`見出し・フッターMENU等）にも似た文言が
  あるため、ここでは`<nav id="globalNav">`〜`</nav>`の範囲内であることを
  確認しながら、上から順に各`<li>`を個別エリアとして登録：
  - [ ] 検索: `>トップ<`（ページ内で一意）
  - [ ] 検索: `>タイセイの特徴<`（ページ内に3箇所。ここが1件目＝nav内）
  - [ ] 検索: `>賃貸物件<`（ページ内で一意）→ この`<li>`はさらに配下の
        `<ul class="subNav">`を同一エリアIDで再帰登録（サブメニュー3項目）
    - [ ] 検索: `>エリアで探す<`
    - [ ] 検索: `>間取りで探す<`
    - [ ] 検索: `>空室一覧<`
  - [ ] 検索: `>売買物件<`（ページ内で一意）→ この`<li>`も同様にサブメニュー2項目
    - [ ] 検索: `>売買物件を探す<`
    - [ ] 検索: `>土地活用のご相談<`
  - [ ] 検索: `>初めての方へ<`（ページ内に3箇所。ここが2件目＝nav内、
        1件目=utilityBar、3件目=フッター）※`toppage.html`/`normalpage.html`
        限定。`blogframe.html`/`blogmain.html`はここが`>お知らせ<`
        （`isActive`付き）になっている
  - [ ] 検索: `>会社案内<`（ページ内に2箇所。ここが1件目＝nav内、2件目=フッター）
- **✅2026-08-19修正**：`>賃貸物件<`／`>売買物件<`の`<li>`直下`<div>`にあった
  `navCaret`svg（ドロップダウン矢印）を廃止し、CSS疑似要素（`::after`の
  border矢印）に置き換え済み。以前は`<div><a>賃貸物件</a><svg>...</svg></div>`
  というdiv1つに性質の違う要素（テキストリンク＋アイコン）が混在しており、
  「1要素として認識されない」エラーが実機で発生した（うにさんの指摘：divで
  囲われていないと1要素にならない、の裏返し）。svgを削除したことで`<a>`は
  再びプレーンテキストのみに戻り、他のnav項目と同じ「単独`<a>`の自動吸収」
  （12節F）で問題なく登録できる想定。あわせて、nav全体（トップレベル・
  サブメニューとも）の`href`を削除済み（リンク先はWebChangerに登録後委ねる
  方針、うにさんの指摘）。

### A-4. フッター
- [ ] 検索: `id="globalFooter"` → 共通エリア `cmn007` 相当（実際の番号はツール自動採番）
- 配下は個別ブロック：
  - [ ] 検索: `class="footerBrand"` → ロゴ・説明文・免許番号
  - [ ] 検索: `>MENU<` → リンクリスト
  - [ ] 検索: `>COMPANY<` → リンクリスト（※`toppage.html`/`normalpage.html`は
        6項目、`blogframe.html`/`blogmain.html`は5項目で内容が微妙に異なる）
  - [ ] 検索: `>CONTACT<` → 住所・電話番号

### A-5. 固定要素（エリア・ブロック化しない、確認だけでOK）
- [ ] `#navToggle`・`#navBackdrop`（ハンバーガーメニューの開閉UI）
- [ ] `#toTop`（ページトップへ戻るボタン）

### A-6. 下部固定バー（`#contactBar`）
- **✅2026-08-19修正**：以前は固定要素扱いの想定（A-5）だったが、うにさんが
  実際に化ツールでエリア化した（変更対応が必要なため）。
- [ ] 検索: `id="contactBar"` → 共通エリア（各`<div class="cbCell">`をその配下の
      個別ブロックとして登録）
  - [ ] 検索: `賃貸のお問い合わせ`（`toppage.html`/`blogframe.html`）または
        `この物件について問い合わせる`（`normalpage.html`）→ プレーンテキストの
        単独`<a>`なので自動吸収でテキストブロックになる想定
  - [ ] 検索: `売買・土地相談` → 同上
  - [ ] 検索: `054-628-4186` → **先回りで`<a>`から`<div class="cbItem cbTel">`に
        修正済み**（`<small>`で電話ラベルを含む1つのdiv）。ロゴの`.logoLink`と
        同じ「入れ子markupを持つ`<a>`は自動吸収されず、`<a>`のままでは1要素として
        認識されない」問題を先回りで回避した

---

## B. ページ固有（各ページを開いたときに個別対応）

### B-1. `toppage.html`

- [ ] 検索: `class="heroSlider"` → 画像スライド標準ブロックを設置（2026-08-19、
      独自JSスライダーから乗り換え済み）。設定値：`data-type="2"`
      （フェード切り替え）・`data-interval="5000"`目安・3〜5枚程度。詳細は
      同じ場所の`[WC]`コメント参照。ドットの見た目調整（`.rdsp-*`へのCSS）は
      「9.統合データ作成」より後、ブロック調整のタイミングでよい
- [ ] 検索: `class="heroTop"` → **✅2026-08-19修正**：`are005`として登録するが、
      `.heroTop`ごと1ブロックにはしない（実機でエリア化失敗、`.sectionHead`と
      同じ「複数要素の混在」問題）。配下の3要素をそれぞれ個別ブロックとして
      登録する：
  - [ ] 検索: `class="heroBadge"` → テキストブロック1つ（アイコンsvgは廃止済み）
  - [ ] 検索: `class="heroTitle"` → 見出しブロック1つ（h2。**✅2026-08-19修正**：
        以前は色付け強調部分を`<span>`/`<div class="accent">`でマークアップして
        いたが、見出しブロック内でプレーンテキストと`<div>`が混在すると「要素が
        1つに認定されない」エラーになったため撤去済み。現在はプレーンテキストの
        みなのでそのまま1ブロックとして登録できる。登録後、「住んでからも、
        任せてからも」の部分をWebChangerのリッチテキストエディタの文字色機能で
        オレンジ色（`#D97425`）に着色する）
  - [ ] 検索: `class="heroLead"` → テキストブロック1つ
- [ ] 検索: `class="heroCards"` → 横並びエリア（`colmode="1"`）
  - [ ] 検索: `FOR TENANTS` → 単体エリア1つを登録（タグ→見出しh3→本文→リンク文言の
        4ブロック。`.hcLink`の矢印svgは廃止済みなのでプレーンテキストとして
        問題なくブロック化できる）
  - [ ] **✅2026-08-19修正**：以前は静的HTML側に2枚とも書いた上で「1枚だけ登録して
        コピー」と案内していたが、これは誤り（化ツールのコピー機能は登録後に
        新規追加する分にしか使えず、既に存在する枚数分は個別登録を要求される。
        13節D・CLAUDE.md 6節参照）。そのため静的HTML側は`FOR TENANTS`の1枚だけに
        縮小済み。**残り1枚は、上の単体エリアを登録した後、化ツール側で
        横並びエリアに単体エリアをもう1つ追加**（2枚に増やせば列数も自動的に
        2列になる、13節D）し、中身を以下の内容で入力する：
        - タグ：`FOR OWNERS`
        - 見出し（h3）：`土地・不動産のご相談`
        - 本文：`売買・土地運用・ソーラー発電・リフォームまで、同じ大家目線で
          ご相談に乗ります。`
        - リンク文言：`オーナー向けサービスを見る`（リンク先はブロックリンク
          機能`rdhaslink`で設定、13節I）
- [ ] 検索: `id="properties"` の中の `<div class="inner">` → `are001`（`mstcon="1"`）
- [ ] 検索: `class="sectionHead"`（`RECOMMENDED`の方、1件目）→ **✅2026-08-19修正**：
      `.sectionHead`ごと1ブロックにはしない（「コンテンツ枠が1つでない」エラーの
      原因）。配下の`.sectionEyebrow`（div、旧spanから変更済み）／`.sectionTitle`
      (h2)／`.sectionLead`をそれぞれ個別に3ブロック登録する
- [ ] 検索: `class="recoGrid"` → 折り返しエリア（`flexmode="1"`）
  - [ ] 検索: `プレステージ 103号室` → 単体エリア1つを登録（画像→タグ→見出し→
        価格→所在地の5ブロック。**✅2026-08-19修正**：`.recoTag`は元々`<span>`
        だったが、実機で「1要素として認識されない」エラーの原因になったため
        `<div>`に変更済み——`<span>`は`<a>`と同じくインライン要素扱いで、
        単独で1ブロックの外枠にはできない、とうにさんの実機検証で判明した）
  - [ ] **✅2026-08-19修正**：静的HTML側は代表1枚（プレステージ103号室）だけに
        縮小済み（理由はheroCardsの項と同じ、13節D・CLAUDE.md 6節参照）。
        登録した1つを化ツール側で8回コピーし、以下の内容にそれぞれ差し替える：
        | タグ | 物件名 | 価格 | 所在地 |
        |---|---|---|---|
        | 格安 | ルミエール 101号室 | 4.0万円（共益費3,000円） | 焼津市東小川3丁目・2DK・西側角部屋 |
        | 格安 | メゾン太盛 201号室 | 4.5万円（共益費3,000円） | 焼津市大覚寺・2LDK |
        | 格安 | メゾン太盛 202号室 | 4.5万円（共益費3,000円） | 焼津市大覚寺・2LDK |
        | 格安 | ルミエール 203号室 | 3.9万円（共益費3,000円） | 焼津市東小川3丁目・2DK |
        | 単身向け | Tアベニュウ 302号室 | 4.2万円（共益費3,000円） | 焼津市三右ェ門新田・1K・9.7畳・最上階 |
        | 単身向け | Tスクエア 101号室 | 3.8万円（共益費3,000円） | 焼津市五ヶ堀之内・1K・1階角部屋・洋8.3畳 |
        | 単身向け | Tスクエア 305号室 | 4.2万円（共益費3,000円） | 焼津市五ヶ堀之内・1K・3階角部屋・洋8.3畳 |
        | 格安 | プレステージ 206号室 | 4.6万円（共益費3,000円） | 焼津市東小川3丁目・2DK・2階 |
- [ ] 検索: `class="noticeBox"` → **✅2026-08-19修正（3回目で最終形）**：
      アイコンは廃止済み。`.noticeBox`自体を共通エリア`cmn001`相当にし、配下の
      h3（見出しブロック）と`.noticeBody`（テキストブロック、`<p>`は使わず`<div>`
      に変更済み）をそれぞれ個別に2ブロック登録する（h3+pをまとめて1つの
      リッチテキストブロックにする案は「コンテンツ枠が1つでない」エラーの
      原因だったため撤回）
- [ ] 検索: `id="features"` の中の `class="bizGrid"` → `are002`
  - [ ] **✅2026-08-19修正**：以前は「1つ登録してコピー」と案内していたが誤り。
        各bizCellはアイコンがカードごとに形が違うため、コピーでは正しいアイコンに
        ならない（うにさんの指摘）。4つとも個別に検索・登録する。**アイコン自体は
        `<svg>`から中身が空の`<div class="bicon biconXxx">`（CSS背景画像）に
        変更済み**（生のsvgが「コンテンツ枠がブロック要素でない」エラーの原因
        だったため）：
        - [ ] 検索: `自社物件・自社管理` → カスタムブロック1つを登録（配下は
              単体エリアの入れ子不要、直接ブロック）
        - [ ] 検索: `トラブルに即時対応` → カスタムブロック1つを登録
        - [ ] 検索: `顔の見える関係` → カスタムブロック1つを登録
        - [ ] 検索: `土地運用のご相談も` → カスタムブロック1つを登録
- [ ] 検索: `id="stats"` → **✅2026-08-19修正**：以前「固定のままでよい」としていたが
      誤り（創業年からの経過年数など、年が変われば数値も変わるためエリア・
      ブロック化が必要、うにさんの指摘）。テキストブロック×4（またはカスタム
      ブロック、8節の数値カウントアップ演出パターン参照）として登録する
- [ ] 検索: `id="business"` の中の `class="bizGrid"` → `are003`
  - [ ] **✅2026-08-19修正**：features側と同じ理由でコピー不可。アイコンも
        features側と同じくsvg→中身が空の`div.bicon.biconXxx`（CSS背景画像）に
        変更済み。4つとも個別登録：
        - [ ] 検索: `不動産賃貸・管理` → カスタムブロック1つを登録
        - [ ] 検索: `売買・仲介・分譲` → カスタムブロック1つを登録
        - [ ] 検索: `ソーラー発電事業` → カスタムブロック1つを登録
        - [ ] 検索: `リフォーム事業` → カスタムブロック1つを登録
- [ ] 検索: `id="philosophy"` → **✅2026-08-19修正**：`are004`として登録するが、
      まとめて1テキストブロックにはしない（`.heroTop`と同じ理由）。配下3要素を
      それぞれ個別ブロックとして登録：
  - [ ] 検索: `OUR PHILOSOPHY` → テキストブロック1つ（`.sectionEyebrow`、
        旧spanから`<div>`に変更済み）
  - [ ] 検索: `理想は、江戸の長屋の大家さん` → テキストブロック1つ（`.philoQuote`）
  - [ ] 検索: `入居者様と大家` → テキストブロック1つ（`.philoBody`）
- [ ] 検索: `id="cta"` → 未エリア化 or `are0XX`（見出し＋リード文）
- [ ] 検索: `id="listingBanners"` → 未エリア化（SUUMO1枚のみ）or 折り返しエリア
      （バナーが複数に増えたタイミングで検討）

### B-2. `normalpage.html`

- [ ] 検索: `id="breadcrumb"` → `are002`（ページごとに個別、通常エリア。
      パンくずリスト標準ブロック）
- [ ] 検索: `id="pageHead"` の中の `<div class="inner">` → **✅2026-08-19修正**：
      `are003`として登録するが、まとめて1テキストブロックにはしない
      （`.heroTop`と同じ理由）。配下4要素をそれぞれ個別ブロックとして登録：
  - [ ] 検索: `class="pageEyebrow"` → テキストブロック1つ（旧spanから`<div>`に変更済み）
  - [ ] 検索: `class="pageTitle"` → 見出しブロック1つ（h2）
  - [ ] 検索: `class="pageLead"` → テキストブロック1つ
  - [ ] 検索: `class="pageNote"` → テキストブロック1つ（アイコンsvgは廃止済み）
- [ ] 検索: `id="rentList"` → `are001`（`mstcon="1"`）
- [ ] 検索: `class="rentGrid"` → 折り返しエリア（`flexmode="1"`）
  - [ ] 検索: `Tアヴェニュウ` → 単体エリア1つを登録（画像→見出し→間取り種別→
        所在地→価格→説明文→空室号室→CTA文言の8ブロック。`.rentCta`の矢印svgは
        廃止済みなのでプレーンテキストとして問題なくブロック化できる。`.rentType`
        〈間取り種別〉も旧spanから`<div>`に変更済み）
  - [ ] **✅2026-08-19修正**：静的HTML側は代表1枚（Tアヴェニュウ）だけに縮小済み
        （理由はtoppage.html側のheroCards/recoGridの項と同じ）。登録した1つを
        化ツール側で4回コピーし、以下の内容にそれぞれ差し替える：
        - **Tスクエア**：間取り1K／静岡県焼津市五ケ堀之内／3.7万円〜（共益費
          3,000円）／西焼津駅徒歩8分のおしゃれな物件。2口コンロのシステム
          キッチンで本格的な料理も可能です。駐車場1台付き。／空室：101・103・
          105・201・202・303・305号室
        - **ルミエール**：間取り2DK／静岡県焼津市東小川3／3.7万円（共益費
          3,000円）／閑静な住宅街に立地。駐車場1台付き、2台目の確保も
          可能です。／空室：101・102・103・201・202号室
        - **メゾン太盛**：間取り2LDK／静岡県焼津市大覚寺／4.5万円（共益費
          3,000円）／東名高速道路沿いですが静かな環境です。／空室：102・201・
          202号室
        - **プレステージ**：間取り2DK／静岡県焼津市東小川3丁目／4.3万円〜
          （共益費3,000円）／格安で入居できる物件です。詳細は各号室の
          お問い合わせよりご確認ください。／空室：103号室（1階）・206号室
          （2階）
- [ ] 検索: `mainAreaAboveCmn` → 保険共通エリア（空、メインの上）
- [ ] 検索: `mainAreaAboveAre` → 保険通常エリア（空、メインの上）
- [ ] 検索: `mainAreaBelowCmn` → 保険共通エリア（空、メインの下）
- [ ] 検索: `mainAreaBelowAre` → 保険通常エリア（空、メインの下）

### B-3. `blogframe.html`

- [ ] 検索: `id="breadcrumb"` → `cmn0XX`（共通エリアのみ設置可、通常エリア不可）
- [ ] 検索: `mainAreaAboveBlogCmn` → 保険ブログ共通エリア（空、メインの上）
- [ ] 検索: `mainAreaBelowBlogCmn` → 保険ブログ共通エリア（空、メインの下）
- [ ] `<main></main>`（中身は空） → エリアではなく`{%%rdblogframe%%}`を配置
      （通常のエリアは設置不可。K-012仕様）

### B-4. `blogmain.html`

- [ ] 検索: `id="breadcrumb"` → `are0XX`（記事ごとに個別、通常エリア）
- [ ] 検索: `class="articleHead"` → ブログ専用の「記事タイトル」「公開日」標準
      ブロックに置き換え（`.rdarticleTitle h3`／`.rdpublishDate`が出力クラス）
- [ ] 検索: `class="articleBody"` → `are001`（`mstcon="1"`）
- [ ] 検索: `class="articleTags"` → WebChanger標準のブログタグ機能
      （`.rdTaglist`）に置き換わる可能性が高い。手動でエリア・ブロック化する
      場合は個別div対応済み
- [ ] 検索: `class="articleNav"` → 前後記事リンクの自動生成機能に置き換わる
      可能性が高い。手動でエリア・ブロック化する場合は個別div対応済み
- [ ] 検索: `mainAreaAboveBlogCmn` → 保険ブログ共通エリア（空、メインの上）
- [ ] 検索: `mainAreaAboveAre` → 保険通常エリア（空、メインの上）
- [ ] 検索: `mainAreaBelowBlogCmn` → 保険ブログ共通エリア（空、メインの下）
- [ ] 検索: `mainAreaBelowAre` → 保険通常エリア（空、メインの下）

---

## 補足

- ここに載っている検索文字列・クラス名は2026-08-19時点のHTMLに基づく。
  HTML側を修正した場合はこのチェックリストも更新すること。
- 各項目の「なぜこの構成にしたか」（例：なぜ折り返しエリア+単体エリアなのか、
  なぜ`<article>`にしたのか等）は`taisei-site-area-block-guide.md`の
  該当節を参照。作業中に判断に迷ったらそちらを見る。
- 化ツールで登録時にエラーが出た場合は、まず
  `taisei-site-area-block-guide.md` 1.12節「実機検証で判明した
  エリア・ブロック化の落とし穴」を確認する（コメント配置・アイコン混在・
  並列裸要素・`<a>`の入れ子などの既知パターンをまとめてある）。
