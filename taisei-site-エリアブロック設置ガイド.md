# taisei-site エリア・ブロック設置ガイド（実装ガイド）

最終更新：2026-08-18。CLAUDE.md 13節時点の知見までを反映。

---

## 0. このガイドの位置づけ

`CLAUDE.md` は「なぜそうするか・WebChangerの制約は何か」を一次資料として蓄積したドキュメント。
このファイルはその応用編で、**`taisei-site/`の各テンプレートを実際にWebChanger化ツールに
通すときに、どのセクションをどのエリア種別にし、中にどの標準ブロックを置き、どのCSSクラスを
どこに付けるか**を、ページ単位・セクション単位で具体的に指示するための実務ガイド。

- 迷ったとき・仕様の根拠を確認したいとき → `CLAUDE.md`（該当節番号をこのガイド内に記載）
- 実際にツールを開いてエリア・ブロックを設定するとき → このガイド

エリアIDの数字（`are001`など）はツール側が設置順に自動採番するものであり、このガイドで
書いている番号は**候補・目安**にすぎない。実際に採番された番号が異なっても問題ない。

---

## 1. 全ページ共通の設計方針

### 1.1 固定要素（エリア化しない）

以下は`taisei-site/`では意図的にエリア・ブロック化しない固定テンプレート要素として扱う。

| 要素 | 理由 |
|---|---|
| `#navToggle`・`.heroDots`（JS生成部分） | JSが編集画面で動かないため、そもそも編集対象にする意味が薄い |
| `#contactBar` | 全ページ共通の定型的な導線。変更頻度が低ければ固定のままでよい（変更したくなったら共通エリア化を検討） |

**`#utilityBar`は方針転換（2026-08-19）**：当初は上記と同じ理由で固定要素扱いだったが、
実際に化ツールで`cmn002`（「ページ最上部」）として共通エリア登録された（実機確認済み）。
登録時、`.ubBadge`（営業時間, span）と2つの`<a>`が並列の裸の兄弟要素だったため1つの
ブロックにまとめて吸収されてしまう問題が発生し、それぞれを個別の`<div>`で囲む対応を
行った（CLAUDE.md 3節参照）。

**`#globalNav`（メインナビ本体）は方針転換（2026-08-18）**：当初は「頻繁に編集する
想定がないため固定HTML扱い」としていたが、ドロップダウンサブメニューを実際に
WebChanger管理画面からオン・オフ／内容編集したいという要望を受けて、**うにさんの
判断でエリア・ブロック化必須**に変更した。詳細は1.11節参照。

### 1.2 ロゴ・フッターは共通エリア候補（sample-project実物の採番例）

`sample-project/`実物比較（CLAUDE.md 12節）で確認された、実際の変換結果の採番パターン：

- ロゴ → `cmn001`（画像ブロック1つ）
- ナビ（使う場合） → `cmn002`以降（多階層メニューは同一エリアIDの再帰参照、12節H）
- フッター → `cmn007`
- 問い合わせ導線（`#footerContact`相当） → `cmn014`

`taisei-site/`のヘッダーロゴ・フッターも同じ発想で共通エリア化候補にできる（実際の番号は
ツールの自動採番に従う。上記は「だいたいこの並びで採番される」という参考値）。
**実機での実際の採番例（2026-08-19確認）**：`taisei-site/`では`#utilityBar`
（ページ最上部）が`cmn002`として登録された。sample-project実物調査時の
「ナビ＝cmn002」という参考値とは対象が異なる採番になっており、実際の割り当ては
案件・登録順によって変わるため、上記の並びはあくまで目安として扱うこと。

### 1.3 パンくずリストエリア（トップページ以外）

**うにさんの実務パターン**：トップページ以外の各テンプレートには、パンくずリスト用の
専用エリアを`</header>`直後に用意するのが通例。

- `normalpage.html`・`blogmain.html`：ページ／記事ごとにパンくず末尾の文言が変わるため
  **通常エリア（`are`）**が適切
- `blogframe.html`：通常エリアを置けない制約があるため**共通エリア（`cmn`）**を使う
  （7節）
- `toppage.html`：起点ページなのでパンくず自体不要

配置するブロックは標準の「パンくずリスト」ブロック（出力クラス`.rdBreadcrumb`、CLAUDE.md
9節・11節`pan.html`）。`taisei-site/`の該当テンプレートには`<div id="breadcrumb">`という
空divを`[WC]`コメント付きで用意済み。

### 1.4 メインエリア上下の保険エリア

**うにさんの実務パターン**：デザインによっては、メインエリアの直前・直後に**空の共通エリア
＋空の通常エリア**を1組ずつ置いておくことがある。公開後に「メインコンテンツの上/下に
セクションを追加したい」という要望が来た際、テンプレート改修なしで対応するための保険。
空の状態では見た目に一切影響しない。

**スコープの分け方（2026-08-18、うにさんの指摘で設計を精緻化）**：素朴に同じ`cmn` IDを
全テンプレートで使い回すと、本来無関係なテンプレート同士（一般ページとブログ）にまで
予備コンテンツが漏れ出てしまう。テンプレートの用途ごとにスコープを分けること：

- **`normalpage.html`**（一般ページ、複数の実ページで使い回すテンプレート）：
  `#mainAreaAboveCmn`・`#mainAreaAboveAre`／`#mainAreaBelowCmn`・`#mainAreaBelowAre`
  を設置済み。この`cmn`は**一般ページ専用の番号**で登録すること（ブログ側とID被り厳禁）。
- **`blogframe.html`／`blogmain.html`**（ブログ系2テンプレート）：`cmn`ではなく
  **ブログ共通エリア（`blogcmn="1"`、12節L参照）**を使う。`#mainAreaAboveBlogCmn`・
  `#mainAreaBelowBlogCmn`という名前の空divで設置済み（`blogframe.html`は通常エリアを
  置けないため`blogcmn`のみ、`blogmain.html`は`blogcmn`＋`are`のペア）。
- **`toppage.html`**：**保険エリアを設置していない**（2026-08-18対応）。トップページは
  サイトの顔として毎回デザインを作り込む前提のページで、「後から思いつきで1セクション
  足したい」という一般ページ・ブログの運用ニーズとは性質が異なるため。案件によっては
  トップページにも欲しい場合があるので、要否はクライアントの運用スタイルに応じて判断すること。

### 1.5 多項目カードの組み方（最重要パターン）

`.recoCard`・`.rentCard`・`.heroCard`のように、1枚の中に画像・見出し・価格・所在地など
**複数の編集可能項目を持つ繰り返しカード**は、以下の構成で組む（CLAUDE.md 13節A-1、
うにさんの指摘で確定した方針）。

```
折り返しエリア（flexmode="1"、列数はツールUIで設定）
└─ 単体エリア（＝カード1枚。通常のエリア。コピー・追加で枚数を増やす）
    ├─ 画像ブロック
    ├─ 見出しブロック（h3など）
    ├─ テキストブロック × 複数（タグ／価格／所在地／説明文など）
    └─ （必要なら）ブロックリンク機能でカード全体をリンク化
```

**カスタムブロックは使わない**。カスタムブロックは「標準ブロック＋クラス追加機能の
組み合わせでは再現できない特殊なコード（inline SVGアイコン埋め込みなど）を使いたい場合」
にのみ使う、という位置づけ（うにさんの発言：「カスタムブロックは特殊なコードを埋め込む
ときくらいしか使わない」）。

編集項目が1つだけ（見出しのみ等）のコンポーネント（`.bizCell`など）は、この制約に
そもそも抵触しないため、カスタムブロック1つで組んでも標準ブロック1つを単体エリアに
置いても、どちらでもよい。

### 1.6 クラスの当て方の基本パターン

**うにさんの実務パターン**：「このクラスのエリアの中の、このクラスのブロックには、
このCSSが効く」という使い方をする。

1. クラス追加機能で、**単体エリア**に固有クラス（例：`recoCard`）を付ける
   （メインエリアには使えないが、配下の子エリア・単体エリアには使える。CLAUDE.md 10節10番）
2. クラス追加機能で、**各標準ブロック**にも個別クラス（例：`recoTag`／`recoPrice`／
   `recoMeta`）を付ける
3. CSSは`common/css/style.css`に既存の`.recoCard .recoTag{...}`のような**子孫セレクタ**の
   まま使える（親要素起点のセレクタ設計、CLAUDE.md 8節）

同じ種類のブロック（テキストブロックはすべて出力クラス`.rdplanetext`共通）を1エリア内で
複数使う場合、**ブロック側の個別クラス付与が区別のために必須**になる点に注意。

### 1.7 h1・見出しの扱い

- 各テンプレートの`<body>`直後にある`<h1 class="visuallyHidden">`が、WebChangerの
  「H1部分の指定」の対象（CLAUDE.md 5節）。デザイン上の見出し（`.heroTitle`
  `.pageTitle`等）はh2/h3であり、H1指定の対象にしない。
- `blogmain.html`の記事タイトル（`.articleTitle`）は標準ブロックの「記事タイトル」
  ブロックに対応し、出力は`.rdarticleTitle h3`（h1ではない）。
- **見出しタグ（h2〜h6）はcommon/css/style.css側で常時ブランドデザインが当たる
  設計にしてある**（2026-08-18、CLAUDE.md 4節参照）。素の`h2`〜`h6`タグ・
  `.rdplanetitle h2`〜`h6`（見出しブロックの出力）どちらも無条件でブランドの
  デフォルトデザイン（font-family・font-weight・color・レベルごとのfont-size）
  が当たるため、新しく見出しブロックを置く箇所でクラス追加を忘れても、
  素の見た目のまま本番に出ることはない。`.sectionTitle`・`.heroCard h3`・
  `.recoBody h3`のような本ガイド記載の個別クラスは、あくまでこのデフォルトからの
  「差分」を表現するための上書き指定であり、指定を忘れても壊れて見えることはない
  （ただしデザインの統一感は個別クラスを付けた方が高い）。デザインを一切
  当てたくない箇所（ヘッダーのロゴ文言等）はCSSで打ち消すのではなく、
  そもそも見出しタグ・見出しブロックを使わない固定要素として組むこと。

### 1.8 本文中のテキストリンクの扱い

- ナビ・ボタン・カードのリンク（`.navUl a`・`.btnPrimary`等）は
  `a{color:inherit;text-decoration:none}`のままでよい（個別クラスで別途
  デザイン済み）。一方、**リッチテキストエディタで本文中に挿入されるリンクは
  素の`<a href="...">`のまま出力される**（CLAUDE.md 13節G）ため、地の文と
  見分けがつかなくなる問題があった。
- 対応（2026-08-18、CLAUDE.md 4節参照）：`.rdplanetext`/`.rdplanetext2`
  （標準テキストブロックの出力クラス）と`.articleBody`・`.noticeBox p`
  （taisei-site独自の本文相当コンテナ）を対象に、通常時は下線なし・色のみ、
  `hover`/`focus-visible`時に下線を追加する設計にしてある。太字は
  `<strong>`と紛らわしくなるため使わない。`:visited`色もこの範囲だけに
  限定し、ナビ・ボタンには適用しない。
- **今後、地の文（自由入力のテキストブロック）が入る新しいコンテナを追加する
  場合は、このリンクスタイルの対象クラスに追加すること**（忘れるとそのコンテナ
  内のリンクだけ地の文に埋もれてしまう）。

### 1.9 ファイルリンク（Excel/PDF/Word等）の種別バッジ

- 賃貸申込書（Excel）や重要事項説明書（PDF）等、太盛工業案件でも今後
  本文中にファイルリンクを置く場面が想定される。WebChangerの
  「クラス追加機能」はブロック単位にしか効かず、地の文中の個別`<a>`を
  狙い撃ちできないため（CLAUDE.md 4節・13節J）、`href`末尾の拡張子で
  自動判定してバッジを表示する方式にしてある。**編集者は普段どおり
  リンクを挿入するだけでよく、追加のクラス指定は不要**。
- 対応済み拡張子：`.pdf`／`.xlsx`・`.xls`／`.csv`／`.docx`・`.doc`／
  `.pptx`・`.ppt`／`.zip`。見た目は`.recoTag`と同じピル型バッジ
  （背景`#FCEBD8`・文字色`var(--mikan-deep)`）に、ドキュメント形の
  インラインSVGアイコンを添えたもの。
- 新しい拡張子（例：`.pages`や`.key`等）に対応させたい場合は、
  `common/css/style.css`の該当ブロック（`a[href$=".pdf" i]::after`等）に
  同じパターンで追記する。
- アップロードされたファイルのURLが拡張子で終わらない特殊な構成の場合は
  自動判定できない（未検証、CLAUDE.md 10節参照）。その場合はリンク文言側に
  「（PDF）」等を手動で書き添えること。

### 1.10 固定UI要素（ページトップへ戻る・ハンバーガーメニューの演出）

いずれも4テンプレート共通の固定要素で、エリア・ブロック化しない
（1.1節と同じ扱い）。

- **`#toTop`（ページトップへ戻るボタン）**：右下固定、`common/js/script.js`が
  `window.scrollY > 480`でクラス`isVisible`を付け外しする。スムーズスクロールは
  `html{scroll-behavior:smooth}`（元々`common/css/style.css`に設置済み）任せで、
  ボタン自体は`window.scrollTo({top:0,behavior:'smooth'})`を呼ぶだけ。
  `#contactBar`（トップ・一般ページ・ブログ一覧に存在するスクロール追従の
  下部固定バー）と重なるため、`body:has(#contactBar) #toTop{bottom:88px;}`で
  そのページだけ位置を押し上げている。内容変更画面ではJSが動かないため
  常に非表示のままだが、装飾用の固定UIチャンプ（編集対象コンテンツではない）
  なので実害なしと判断し、`#editView`向けの強制表示フォールバックはあえて
  追加していない。
- **`#navBackdrop`（メニューを開いた時の背景幕）＋メニュー項目のstagger演出**：
  `#navToggle`クリック時に`common/js/script.js`が`#navBackdrop`にも`isOpen`を
  トグルし、背景を薄暗くフェードインさせつつ`.navUl>li`を上から順にずらして
  フェードインさせる（「ちょっとおしゃれな動き」の要望への対応、2026-08-18）。
  背景幕クリックでも閉じられる（`navBackdrop`のクリックで`closeNav()`）ほか、
  Escapeキーでも閉じられる。ナビ本体（実際のコンテンツであるリンク）は
  JSが動かない編集画面で開いた状態にならず初期状態のopacity:0のまま埋もれて
  しまうため、`.reveal`と同じ理屈で`#editView .navUl>li{opacity:1;transform:none;}`
  を追加済み。`#navBackdrop`自体は装飾要素なので強制表示していない。
- **重要な発見（2026-08-18）**：`#globalHeader`に付けていた`backdrop-filter`
  （すりガラス効果）が、子孫の`#globalNav`の`position:fixed`を壊す不具合が
  あった（`backdrop-filter`はfixed子孫のcontaining blockになる、というCSS仕様。
  CLAUDE.md 4節参照）。ヘッドレスブラウザで実際にメニュー展開状態を
  レンダリングして発見し、`backdrop-filter`を`#globalHeader::before`疑似要素に
  移して解消した。**この手のコンポーネント（アコーディオン・モーダル等、
  「開いた状態」が通常のページロードでは現れないUI）を追加・変更した場合は、
  可能なら実際にその状態をスクリーンショット等で確認すること**。

### 1.11 メインナビ全体をエリア・ブロック化する方針（2026-08-18・方針転換）

**うにさんの判断で、`#globalNav`（メインナビ本体）は全体をエリア・ブロック化する
方針に変更した**。ドロップダウンサブメニュー（賃貸物件・売買物件）を実際に
WebChanger管理画面から内容編集・表示非表示できるようにするには、ナビ自体が
WebChangerのエリア・ブロックの仕組みに乗っている必要があるため（固定HTMLの
ままでは管理画面からの操作対象にならない）。当初の「頻繁に編集する想定がないため
固定HTML扱い」という判断（1.1節に記載していた）は撤回する。

- **★2026-08-19・「親エリア→直下を子エリア→配下は全部ブロック」の2階層ルール
  （CLAUDE.md 6節）に対する意図的な例外**：このナビの実装方式（下記）は、`<li>`
  というエリアのさらに配下に`<ul class="subNav">`という別のエリアを入れ子にする、
  2階層ルールを超えた多階層構造になっている。うにさんに確認の上、この点は例外として
  維持することを確定した。理由：サブメニュー項目を個別にテキスト編集したい・
  「エリアをコピーして項目追加」機能で項目数を増減させたいという運用要望を
  満たすには、サブメニュー自体もエリアである必要があるため（1つのカスタムブロックに
  まとめると項目の個別編集・増減にコード修正が必要になり不便）。それ以外の箇所
  （`.recoGrid`/`.rentGrid`/`.heroCards`/`.bizGrid`等）は全てこの2階層ルールの
  範囲内に収まっている。
- **実装方式**：sample-project実物比較で確認済みの「多階層メニューは同一エリアIDの
  再帰参照で表現される」パターン（CLAUDE.md 12節H）に沿って登録する：
  - `<ul class="navUl">`（メニュー項目リストそのもの）＝共通エリア候補（`cmn`、
    `useronly="1"`。`<ul>`の子は`<li>`のみに制限されるタグ固定タイプのため
    13節C参照）。
  - 各`<li>`＝その配下のエリア（項目1つぶんのリンク）。
  - ドロップダウンを持つ`<li>`の中の`<ul class="subNav">`は、**トップレベルの
    `<ul class="navUl">`と同一のエリアIDを再帰的に参照**することで、追加の
    エリア定義なしに何階層でも同じ仕組みでサブメニューを表現できる
    （sample-projectの`cmn004`が`globalNavSub`・`globalNavMain`の2箇所から
    参照されていたのと同じ考え方）。
  - 実際のエリアID・useronly属性の最終的な割り当ては、化ツールでの登録作業時に
    確定させる（このリポジトリのHTMLに`{%%rdarea%%}`等を手書きする必要はない、
    2節参照）。現状の`<ul class="navUl"><li>...</li></ul>`という素朴な入れ子
    構造のまま、化ツールのGUIで該当箇所を選択・登録すればよい形にしてある。
- **これにより「普段はWebChangerで非表示にしておく」が文字通り実現できる**：
  ナビがブロック化されれば、WebChangerの「表示・非表示」機能（ブロック単位の
  機能、CLAUDE.md 13節K）がそのまま使える。ドロップダウンの中身（サブメニュー
  項目）を空のまま公開しておき、必要になったら管理画面から項目を追加する、
  という運用も可能（6節の「保険エリア」と同じ考え方）。
- **中身**（仮の項目、実データが決まったら管理画面で差し替え）：
  - 賃貸物件：エリアで探す／間取りで探す／空室一覧
  - 売買物件：売買物件を探す／土地活用のご相談
- **見た目・演出**：ホバー時にカード状のパネルがフェード＋わずかに下からスライド
  インしながら表示され、パネル内の項目も上から順にずれてフェードインする
  （ハンバーガーメニューのstagger演出と同じ考え方）。ドロップダウンを持つ項目には
  下向き矢印（`.navCaret`）が付き、ホバー中は180度回転して上向きになる。
  この見た目は、エリア化後も配下の単体エリア・ブロックへのクラス追加機能
  （8節・13節A-1で確立済みの「エリアのクラス配下のブロックのクラス」パターン）
  でそのまま再現できる想定。
- **`hasSubNav`クラスについて（開発時プレビュー用に格下げ）**：
  `common/css/style.css`の`.hasSubNav`（`<body>`に追加すると有効化）は、
  化ツールを通す前の**コーディング段階で見た目を確認するための開発用トグル**として
  残してある。本番でのオン・オフはWebChanger管理画面の「表示・非表示」機能で
  行う想定のため、化ツール変換後はこのクラス自体は使われなくなる（当初
  「これが本番用の切り替え手段」としていたが、ブロック化方針への転換に伴い
  位置づけを訂正した）。
- **未対応・要注意**：ホバー表示は`@media(min-width:981px)`でデスクトップ幅のみに
  スコープしてあり、モバイル（ハンバーガーメニュー）側は`hasSubNav`を有効化しても
  サブメニューが一切表示されない（タップでの開閉挙動が未実装のため）。実際に
  化ツールで変換・運用を始める際は、`common/js/script.js`にモバイル用のタップ
  開閉処理を追加すること（編集画面ではこのJS自体動かない点にも注意、8節参照）。
- **オンマウス時のアニメーション**：ドロップダウンの有無に関わらず、全ての
  トップレベルのナビリンクに、背景色の変化に加えて中央から左右に伸びる下線
  アニメーションを追加した（「色が変わるだけじゃない気の利いたアニメーション」の
  要望対応、2026-08-18）。モバイル幅では`::after`の下線と矢印アイコンを
  非表示にしている（一覧表示のリストデザインと馴染まないため）。

### 1.12 実機検証で判明したエリア・ブロック化の落とし穴（2026-08-19）

うにさんが実際に化ツールでヘッダー・ナビ周りのエリア・ブロック登録を行った際に
出たバリデーションエラーから判明した、1.11節の計画だけでは見えなかった注意点。
以降ヘッダー・ナビ以外の箇所（1.5節のカードパターン等）をエリア・ブロック化する
際にも当てはまる一般的な注意点として扱うこと。

- **エラー「エリア直下は全てブロック要素で構成されていなければならない」**：
  エリアの直下に、①ブロック化されていない生のHTMLコメント、②中身が空・
  ブロック化されていないdiv（固定UI要素のつもりで置いていたもの）が混ざって
  いると発生する。実際に`<header>`・`<div class="inner">`をエリアにしてしまうと、
  中の`#logo`（固定ロゴ）・`#navBackdrop`（固定の背景幕、空div）・`[WC]`ガイド
  コメントがこの条件に引っかかった。**対策**：エリアの開始位置は、固定要素を
  巻き込まない最小限の範囲にする（今回であれば`<header>`や`div.inner`ではなく
  `<nav id="globalNav">`直下の`<ul class="navUl">`）。`[WC]`コメントも、
  エリアにする可能性がある要素の外側に置く。
- **エラー「編集ブロック配下のコンテンツ枠がブロック要素でない」**：
  単独の`<a>`をテキストブロックへ自動吸収する仕組み（12節F）は、`<a>`の中身が
  プレーンテキストのみの場合しか働かない。`<a>賃貸物件<svg class="navCaret">...
  </svg></a>`のようにアイコン（svg）とテキストが`<a>`内に混在していると、
  自動吸収の対象として認識されずこのエラーになる。アイコンなしの`<a>トップ</a>`
  では発生しなかったことからこの原因が特定できた。**対策**：アイコンを`<a>`の
  外（同じ親div内の兄弟要素）に出し、`<a>`はプレーンテキストのみにする。
  見た目（アイコンとテキストの横並び）はCSS側（親divを`display:flex`に）で
  維持できる。`#logo`（ロゴ画像＋テキストが`<a>`に混在）も同種の構造だが、
  そもそもロゴはエリア化想定外の固定要素なので、上記のエリア範囲の是正で
  解消する想定。
- 上記2点を反映し、`taisei-site/`の4テンプレート共通で①`[WC]`コメントを
  `<header>`の外に移動、②ドロップダウン項目（賃貸物件・売買物件）の
  `navCaret`svgを`<a>`の外に出す、のHTML修正とCSS調整を実施済み
  （2026-08-19）。CLAUDE.md 3節・13節Bにもこの実機検証結果を反映した。

---

## 2. ページ別ガイド

### 2.1 `toppage.html`

| セクション | エリア候補 | 中身 | クラス追加候補 | 備考 |
|---|---|---|---|---|
| `#logo`（ヘッダー） | `cmn001`（共通エリア） | 画像ブロック1つ | - | sample-project実物の採番例（1.2節） |
| `#hero .heroSlider` | 未エリア化 or `are0XX` | 画像スライド標準ブロック（`.rdslide`、`data-type="2"`フェード切り替え）を推奨（2026-08-18・方針転換、CLAUDE.md 9節） | - | ヒーローが左右分割レイアウトになりスクリム・テキスト重ねが無くなったため標準ブロックとの相性が良くなった。ドット（`.rdsp-*`）にブランドCSS上書きが必要。乗り換え時は独自JS（`common/js/script.js`のheroSlider制御部分）を撤去し`data-interval`等の属性で制御する |
| `.heroTop`（バッジ・見出し・リード） | `are005`候補 | テキストブロック1つ | - | h1はbody先頭の隠しh1が別途担当（1.7節） |
| `.heroCards` | 横並びエリア（`colmode="1"`） | 単体エリア×2、各内部に見出し→タグ→本文の標準ブロック | エリアに`heroCard`、ブロックに`hcTag`相当のクラス | 1.5節のパターン。カード全体のリンクはブロックリンク機能 |
| `#properties`（メイン） | `are001`（`mstcon="1"`） | `.recoGrid`＋`.noticeBox` | - | `data-prc=""`を親divに付与。新着情報・RSSの概要もここから自動生成される |
| `.recoGrid` | 折り返しエリア（`flexmode="1"`、列数3） | 単体エリア×9（カード数）、各内部に画像→タグ→見出し→価格→所在地の標準ブロック | エリアに`recoCard`、ブロックに`recoTag`/`recoPrice`/`recoMeta` | 1.5節のパターンそのもの |
| `.noticeBox` | `cmn0XX`（共通エリア） | テキストブロック1つ | - | 複数ページで使い回す可能性がある注意書きのため共通エリア |
| `#features`／`#business` | `are002`／`are003` | `.bizGrid`内に横並びor折り返しエリア、配下は単体エリアまたはカスタムブロック×4 | エリアに`bizCell` | `.bizCell`は編集項目がh3見出し1つだけなのでカスタムブロック化も可（1.5節末尾） |
| `#stats` | 未エリア化（固定数値） | - | - | 実績数値は更新頻度が低ければ固定でよい。編集可能にしたい場合はテキストブロック×4。数値はスクロール表示時にJSでカウントアップする演出付き（2026-08-18、CLAUDE.md 8節参照）。編集画面ではJSが動かないため静的な最終値がそのまま表示される安全設計 |
| `#philosophy` | `are004`候補 | テキストブロック1つ | - | 引用文＋本文をまとめて1ブロックでもよい |
| `#cta` | 未エリア化 or `are0XX`候補 | テキストブロック（見出し＋リード文） | - | トップページには保険エリアを置いていない方針（1.4節参照）のため、将来の差し替えが必要ならこのセクション自体をエリア化する |
| `#listingBanners` | 未エリア化（現状SUUMO1枚のみ） or 折り返しエリア（`flexmode="1"`） | バナー画像（`.imgPlaceholder`で仮置き） | エリアに`listingBanner` | 2026-08-19追加。外部不動産ポータル（SUUMO等）のバナー掲載枠。複数枚に増える見込みのため、増えたタイミングで`.recoGrid`と同じ13節A-1のパターン（折り返しエリア＋単体エリア×バナー数、各内部に画像ブロック1つ）でエリア・ブロック化するのがおすすめ。バナー画像は各ポータル指定のトラッキングURL付き素材になることが多い点に注意 |
| フッター | `cmn007`（共通エリア） | フッターリンク・住所等 | - | sample-project実物の採番例（1.2節）。2026-08-19、520px以下（スマホ）でブランド情報とCONTACTを中央寄せ・MENU/COMPANYを2カラム横並びにするCSSを追加し、従来の「全項目が左寄せで縦に長い」レイアウトを解消 |
| `#footerContact`相当 | `cmn014`（共通エリア） | - | - | 現状`taisei-site/`にはこの位置に該当要素なし。将来追加時の参考番号 |

### 2.2 `normalpage.html`

このテンプレートは会社案内・売買物件一覧・初めての方へ 等、複数ページで使い回す前提。

| セクション | エリア候補 | 中身 | クラス追加候補 | 備考 |
|---|---|---|---|---|
| `#breadcrumb` | `are002`候補（通常エリア） | パンくずリスト標準ブロック | - | 1.3節。ページごとに内容差し替え |
| `#pageHead` | `are003`候補 | 見出し・リード文のテキストブロック | - | ページごとに内容差し替え |
| `#rentList`（メイン） | `are001`（`mstcon="1"`） | `.rentGrid` | - | `data-prc=""`を`.inner`に付与 |
| `.rentGrid` | 折り返しエリア（`flexmode="1"`、列数2） | 単体エリア×N（カード数）、各内部に画像→見出し→間取り種別→所在地→価格→説明文→空室号室の標準ブロック | エリアに`rentCard`、ブロックに`rentType`/`rentAddr`/`rentPrice`等 | 1.5節のパターン。空室号室（`.rentRooms`）は複数号室を改行区切りの1テキストブロックにまとめる想定 |

**H1の運用に注意**：このテンプレートを流用する各ページ（会社案内・売買物件一覧等）は、
ページごとに「H1部分の指定」をやり直す必要がある（対象ページ選択はページ単位の作業、
CLAUDE.md 5節）。

### 2.3 `blogframe.html`

**制約**：通常エリア（`are`）は一切置けない。設置できるのは共通エリア／ブログ共通エリア／
ブログフレームタグ（`{%%rdblogframe%%}`）のみ（CLAUDE.md 7節）。

| セクション | エリア候補 | 中身 | 備考 |
|---|---|---|---|
| `#breadcrumb` | `cmn0XX`（共通エリア） | パンくずリスト標準ブロック | 通常エリア不可のため`cmn`（1.3節） |
| `#pageHead` | `cmn018`相当（ブログ共通エリア、`blogcmn="1"`） | 見出し・リード文 | sample-project実物で`cmn018`＋`blogcmn="1"`という第3のエリア共有スコープを確認済み（CLAUDE.md 12節L） |
| `<main>`（メイン） | エリア化不可、`{%%rdblogframe%%}` | システム生成の記事一覧 | 2026-08-19、うにさんの指示でモックアップ（`.blogGrid`〜`.blogPager`、記事カード3件分）は削除済み。`<main>`は空のまま、化ツールで`{%%rdblogframe%%}`を直接配置する。実運用の見た目は`.rdnewArticle`系クラスで調整（CLAUDE.md 7節） |

### 2.4 `blogmain.html`

| セクション | エリア候補 | 中身 | 備考 |
|---|---|---|---|
| `#breadcrumb` | `are0XX`候補（通常エリア） | パンくずリスト標準ブロック | 記事ごとに末尾（記事タイトル相当）が変わるため`are`（1.3節） |
| `.articleHead` | ブログ専用ブロック | 「記事タイトル」ブロック（出力`.rdarticleTitle h3`）＋「公開日」ブロック（`.rdpublishDate`） | h1はbody先頭の隠しh1が別途担当 |
| `.articleShot` | 画像ブロック | - | - |
| `.articleBody`（メイン） | `are001`（`mstcon="1"`） | テキストブロック | `data-prc=""`を`.inner`に付与 |
| `.articleTags` | ブログ専用「タグ一覧」ブロック相当 | - | 標準ブロック仕様書2回目以降で詳細確認予定 |
| `.articleNav` | ブログ専用「前後記事」ブロック相当 | - | 同上 |

---

## 3. クラスリファレンス（`common/css/style.css`抜粋）

実際にクラス追加機能で使う可能性が高い主要クラスと、その付け先・用途の一覧。
新しいクラスを追加した際はこの表にも追記していく運用とする。

| クラス | 付け先の目安 | 用途 |
|---|---|---|
| `heroCard` | 単体エリア | ヒーロー2枚カードの外枠（枠線・背景・padding） |
| `hcTag` | テキストブロック | カード上部の小タグ（FOR TENANTS等） |
| `recoCard` | 単体エリア | おすすめ物件カードの外枠 |
| `recoThumb` | 画像ブロック側の親相当 | カード内画像の表示枠（76×76px固定） |
| `recoTag` | テキストブロック | 「格安」「単身向け」等の小タグ |
| `recoPrice` | テキストブロック | 価格表示（`<small>`込みでOK） |
| `recoMeta` | テキストブロック | 所在地・間取り等の補足情報 |
| `rentCard` | 単体エリア | 賃貸物件一覧カードの外枠 |
| `rentType` | テキストブロック | 間取り種別バッジ（1K等） |
| `rentAddr` | テキストブロック | 所在地 |
| `rentPrice` | テキストブロック | 価格 |
| `rentDesc` | テキストブロック | 説明文 |
| `rentRooms` | テキストブロック | 空室号室リスト（改行区切り） |
| `bizCell` | 単体エリア or カスタムブロック外枠 | 特徴／事業案内カードの外枠 |
| `sectionEyebrow`／`sectionTitle`／`sectionLead` | テキスト・見出しブロック | セクション見出し3点セット |

---

## 4. 変換前チェックリスト

実際にWebChanger化ツールに通す直前に確認する項目（CLAUDE.md 7章のサマリ表と対応）。

- [ ] 4テンプレート全てに`<html class="cmshtml">`・`<body class="cmsbody">`が付いているか
- [ ] `<p>`タグでエリア・ブロック候補を囲んでいないか（純粋なテキストのみに限定）
- [ ] body直下に直接エリアを置いていないか（1階層divで囲む）
- [ ] 繰り返し要素（カード等）に`id`を使っていないか（`class`のみで構成）
- [ ] `common/css`配下に外部CDN参照のCSS/JSが混ざっていないか
- [ ] 汎用`button`セレクタで位置指定をしていないか（`#navToggle`等に個別スコープ済みか）
- [ ] `position:fixed`を使っている箇所に`#editView`向け`static`フォールバックがあるか
- [ ] JS依存の初期非表示要素（`.reveal`等）に`#editView`向け強制表示CSSがあるか

---

## 5. 更新履歴・今後の追記予定

- 2026-08-18：初版作成。CLAUDE.md 13節（標準ブロック仕様書1回目・44ファイル）までの
  知見を反映。
- 標準ブロック仕様書の2回目・3回目（地図・Youtube・SNS4種・ブログ関連11ブロック・
  お問い合わせ表示枠等）が届き次第、該当セクションの表を更新すること。
- 実際にWebChanger化ツールで変換した後、`.recoThumb`/`.recoBody`等の左右分割CSSが
  実出力でどう再現できるかを確認し、3章のクラスリファレンスと合わせて更新すること。
