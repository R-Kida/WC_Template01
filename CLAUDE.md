# WebChanger CMS 仕様ガイド

株式会社グローバルゲートのCMS製品「WebChanger」向けに、HTML/CSS/JSでサイトを
コーディングし、WebChanger化ツールに通す際の技術的な制約・仕様をまとめた
**案件非依存の一般リファレンス**。

複数の社内資料・実サイト調査・実プロジェクトサンプルの実物比較から得られた知見の
集大成。**新しいWebChanger案件でコーディングを始める前に、必ず目を通すこと**。

このファイルはClaude Codeがリポジトリを開くたびに自動で読み込む`CLAUDE.md`として
配置している。特定の案件に関する固有情報（クライアントデータ・デザイン・進捗など）
は別ファイルにまとめる（現在この`WC_Template01`リポジトリでは太盛工業／
`taisei-site/`案件の情報を`taisei-site-プロジェクト情報.md`に、その案件向けの
実装ガイドを`taisei-site-エリアブロック設置ガイド.md`にまとめている）。
新しい案件を始める場合は、このファイルはそのまま流用し、案件固有ファイルだけを
新規に作成すること。

**最終更新**：2026-08-18。標準ブロック仕様書（全56ファイル中、1回目・44ファイル分）
の精読結果まで反映済み。以降の作業でこのファイルの内容と実機の挙動が食い違う場合は、
実際にWebChanger化ツールを動かした結果を優先し、このファイルを更新すること。

本文中で「◯◯節」とだけ書かれている参照は、このファイル内の節番号（1〜13）を指す。
`taisei-site-プロジェクト情報.md`のように別ファイル名が明記されている参照は、
その案件固有ファイル側の章番号を指す。

---

## 1. 基本用語・全体像

- **WebChanger化**：既存の完成されたHTML/CSS/JS一式を「テンプレート」として
  WebChanger化ツールに読み込ませ、コンテンツ編集可能な状態に変換する作業。
- **テンプレートセット**：最低でも「トップページ用」「一般ページ用」の2種類の
  HTMLが必要。加えてブログを使うなら「ブログ一覧（ブログフレーム）用」
  「ブログ記事（ブログメイン）用」も用意する。
- **エリア（`{%%rdarea%%}`）**：ページ内でコンテンツ編集可能な領域。
  - 通常エリアID：`are` + 数字3桁（例：`are001`）
  - 共通エリアID：`cmn` + 数字3桁（例：`cmn001`）。複数ページで同じ内容を
    使い回したい場合に使う（例：太盛工業の「お問い合わせについてのお願い」文言）。
  - メインコンテンツに指定するエリアは1テンプレートにつき1つだけ。
    `mstcon="1"`を付け、親タグに`data-prc=""`を付与する。
  - エリア種別：通常／横並び（`rdflexarea`）／折り返し（`rdhorizontalarea`）
- **ブロック**：エリア内に配置される個々のコンテンツ単位。
  - 標準ブロック：システムにあらかじめ用意されているブロック
    （シンプルテキスト、画像、スライドショー、見出し、地図、YouTube、
    表、ナビゲーション、パンくずリスト、ブログ関連各種、SNS埋め込み等）。
  - ユーザーブロック（カスタムブロック）：開発側が独自にコーディングして
    登録するブロック。繰り返しの多いカード型コンテンツなどはこちらが確実。
    ファイル名規則：`{エリアID}_blk{番号3桁}.html`
    （例：`are001_blk001.html`）。1行目はHTMLコメントで
    `<!-- ブロック名@アイコン種別 -->`（省略可、省略時`etc01`。この行は
    出力時に除去される）。
  - 標準ブロックはすべて`<div>`前提で作られているため、標準ブロックを使う
    エリアは子要素に`<div>`が入る想定で組む。

## 2. 制作フロー（K-006・K-008の要点）

1. 通常の手法でHTML/CSS/JS一式（完成されたWebサイト）を用意する
   ＝ここまでの`taisei-site/`一式がこれにあたる
2. WebChanger化ツールに読み込ませる
3. 対象ページ選択（トップ／一般ページ／ブログフレーム・ブログメインのペア、
   など）
4. エリア・ブロックの設定（GUI上でHTMLソースを見ながら該当箇所を選択・登録。
   この段階でユーザーブロックのHTMLファイルも登録する）
5. H1部分の指定（後述5）
6. テンプレート用HTML・テンプレート定義ファイル（`index.dat`）・
   ユーザーブロック用HTMLファイルの定義と生成 → **これはツールが自動生成する**。
   人間／Claude Codeが事前に手動で用意する必要はない（誤って`index.dat`や
   `*.blk`フォルダを作ってしまい、後で削除した経緯あり。`taisei-site-プロジェクト情報.md`4章参照）
7. 生成物のアップロード・反映

## 3. HTML制作ルール

- `<html>`に`class="cmshtml"`、`<body>`に`class="cmsbody"`が必要
  （**確認済み・対応済み**：`sample-project/01MstData/`〈変換前の完成品サンプル〉の
  段階ですでに`<html lang="ja" class="cmshtml">`・`<body class="cmsbody">`が
  手書きされている。つまりツールが自動付与するのではなく、**コーディング時点で
  自分で付けておくのが正しい作法**。`taisei-site/`の4テンプレート
  〈`toppage.html`/`normalpage.html`/`blogframe.html`/`blogmain.html`〉全てに
  2026-08-18に追加済み、`taisei-site-プロジェクト情報.md`7章参照）
- 不要な空タグ・改行・コメントタグを使わず、必要最低限のHTMLだけにする
  （**最終テンプレートについての話**。変換作業中のガイドコメントは一時的な
  使用としてはOKと判断し、現在`taisei-site/`の各HTMLには`<!-- [WC] ... -->`
  形式のガイドコメントを残している。変換完了後は削除する想定）
- **`<p>`タグの制限が最重要**：入れ子構造の親になる要素（後でエリア・ブロックが
  入る想定の箇所）は`<p>`で囲ってはいけない。純粋なテキストのみの箇所だけ
  `<p>`が使える。違反例と対処法は9節「HTML・CSS修正事例」を参照。
- `id`の使用に注意：ブロックはコピーされてサイト内に複製されるため、
  繰り返し要素（カードなど）に`id`を使うと重複の原因になる。
  → `taisei-site/`内では、繰り返しカード（`.rentCard`、`.recoCard`等）は
  すべて`class`のみで構成し、`id`は1ページに1つしかない構造要素
  （ヘッダー・フッター・各セクション）にのみ使用済み。
- **body直下のタグを親エリアに指定することはできない**。親エリアにしたい
  要素は必ず`<div>`等で1階層余分に囲むこと。

  ```html
  <!-- NG -->
  <body>
    {%%rdarea id="are001" name="メインコンテンツ" mstcon="1" %%}
  </body>

  <!-- OK -->
  <body>
    <div>
      {%%rdarea id="are001" name="メインコンテンツ" mstcon="1" %%}
    </div>
  </body>
  ```

- **テンプレートに直接書かれたimg（ブロック化しないimg）のsrc属性は、
  `/`始まりの絶対パスに変換される**。画像自体は実際には
  `/data/common/c顧客ID/img/`フォルダにアップロードされる仕組みのため、
  そのままでは書き換え後にリンク切れになる。対処法は3つ：
  1. 該当パス（`/data/common/c顧客ID/img/...`）に事前に書き換えておく
  2. テンプレートに直接imgタグを書かない
  3. CSSで背景画像として配置する
  → `taisei-site/`では現状、実写真がまだ用意されていないため
  `.imgPlaceholder`（斜線ハッチング＋アイコン＋テキストのダミー）で
  代用している。実写真差し込み時にこのルールを踏まえて配置方法を決める必要あり。

- **並列に並ぶ要素は入れ子でラップする**：
  - テキストと並列に並ぶimg → 別々のdivで囲む
  - 複数の並列img → それぞれ個別のdivで囲む
  - `<a>`と並列に並ぶ`<li><ul>`（サブメニュー階層） →
    `<a>`をdivで囲む
  - 並列の`<p>`（複数） → それぞれ個別のdivで囲むか、
    1つの`<p>`にまとめて`<br>`区切りにする

  ```html
  <!-- NG -->
  <p><img src="" alt="" class="left" />あいうえお</p>

  <!-- OK -->
  <div>
    <div class="left"><img src="" alt="" /></div>
    <div>あいうえお</div>
  </div>
  ```

- **クラス付きの`<span>`は、内容変更でテキストエディタを一度でも確定操作すると
  削除される**。文字装飾をしたい場合は`<div class="xxx" style="display:inline;">`
  に変更するか、WebChangerの文字装飾機能自体を使うこと。

  ```html
  <!-- NG（内容変更確定時にclassが消える） -->
  <span class="bigtext">おおきなもじ</span>

  <!-- OK -->
  <div class="bigtext" style="display: inline;">おおきなもじ</div>
  ```

- **エリアマップ（`<map><area>`）は編集する方法がないため、固定ブロックにする**
  （エリア・ブロック化しない）。

- **table**：`<table>`タグ（とその配下）をエリアにすると、内容変更画面上で
  タグが大幅に改変される。CSSによる制御が難しい場合は次のいずれかで対応：
  1. 他のタグ（div等）で代用する
  2. テキストブロックの表機能を使って入力し直す
  3. ブロック装飾（枠線・背景）機能で見た目だけ代用する

- **文字への装飾（font-weight/font-style/text-decoration）**：WebChangerの
  文字装飾機能には「装飾をしない」という指定がない（2021年8月時点）。
  そのため、編集可能なテキストの親クラスに`font-weight:bold`等を指定してしまうと、
  お客様側でその装飾を一切消せなくなる。`<strong>/<i>/<u>`タグを直接使うのが
  無難。ただし、丁寧にCSSで作り込んだ見出しなど「お客様に装飾変更させたくない」
  意図がある場合はこの限りではない。

## 4. CSS制作ルール

- **全セレクタが`.cmshtml`配下に自動的に修正される**（WebChanger化ツールが
  自動で階層化する。手動で`.cmshtml`プレフィックスを付ける必要はない
  ＝現状の`common/css/style.css`はプレフィックスなしで統一済み、これで正しい）。
- `body`タグに直接あてるCSSは、`body{...}`のまま書いてよい
  （**確認済み**：`sample-project/`実物比較で、変換ツールが`body{...}`を
  自動的に`.cmsbody{...}`へ書き換えることを確認済み。手動で`.cmsbody`に
  直しておく必要はない。10節1番・12節B参照）。
- 固定装飾用のCSS・IMG・JSはそれぞれ単一フォルダ（`common/css`・`common/img`・
  `common/js`）にまとめる。フォント同梱時はフォントファイルも`css`フォルダ内、
  動画ファイルは`img`フォルダ内に置く仕様。
- **外部CDNのCSS/JSは使用不可というのが建前**（変換時に警告が出る）。ただし
  実務上は、Google Fonts・絵文字・ハンバーガーメニュー等の一般的なアイコン
  フォント（Font Awesome等）といった、業界で広く使われている定番のCDN
  リソースは実際には使われている（2026-08-18、うにさんの実務知見）。
  「絶対に一切使えない」という理解ではなく、**「基本方針としては避け、
  `common/css`・`common/js`への同梱に置き換えるのが望ましいが、Google Fonts等
  定番どころは現場の運用として許容されているケースがある」**という温度感で
  理解しておくこと。ただし警告が出る仕様自体は変わらないため、案件ごとに
  クライアント・案件担当者の判断を仰ぐのが無難。`taisei-site/`では
  Google FontsのCDN参照をやめてシステムフォントに切り替えた経緯がある
  （`taisei-site-プロジェクト情報.md`3.3節参照）が、これは「CDNが技術的に
  一切使えないから」ではなく、このプロジェクトでの個別判断だった点に注意。
- **汎用`button`セレクタや`#何か button`のような広いセレクタでボタンの位置
  （position/top/right/bottom/left/width等）を指定してはいけない**。
  WebChanger自身が内容変更画面に出す「編集する」ボタン（YouTube/マップ埋め込み
  時など）も実体は`<button>`タグであり、同じDOM階層に描画されるため、
  意図せず位置がずれて表示崩れを起こす（K-010で詳述、実際に自分のCSSにあった
  `button{font-family:inherit;cursor:pointer;}`を`#navToggle`・
  `.heroDots button`に個別スコープし直した経緯あり）。
  もし今後同様の崩れが起きた場合の直し方：
  1. ブラウザDevToolsで内容変更画面を開きF12
  2. Elements/Stylesタブで実際に効いているCSSを特定
  3. `#editView .cmshtml #contents_inner #main .btn-edit`（または
     `.btn-editYoutube`等のより限定的なクラス）配下に絞った修正CSSを
     `common/css/style.css`に追記

## 5. H1の特殊仕様（重要・修正済み事項）

- WebChangerのH1は、通常のエリア／ブロックとは別の**「H1部分の指定」という
  特殊な化ツール操作**で設定する。
- 対象は**プレーンテキストのみ**（`<br>`や`<span>`などの入れ子markupを含む
  箇所は選択対象にできない）。alt属性への設置も可能。
- 手順：化ツールでページを開き、対象テキストを選択 →
  「選択部分から内容取得＆H1タグ設置」をクリック → 変換後は
  `{%%rdtplblk_h1%%}`に置き換わり、以後は通常の内容編集画面のブロックとしては
  出てこず、「ヘッダ等情報編集」という別画面から編集する。
- 実サイト（`taisei-kougyou.net`旧版）は`<h1 class="globalh1">確かな技術と
  経験で世界を支える株式会社サンプル商店</h1>`のように、**視覚的には隠れた
  SEO用h1をbody先頭に別途置き、デザイン上の見出しは別タグ（h2以下）を使う**
  構成だった。

**この方針に合わせて`taisei-site/`は全面的に修正済み**：

- `toppage.html` / `normalpage.html` / `blogframe.html`：`<body>`直後に
  `class="visuallyHidden"`（sr-onlyパターン、CSSで視覚的に隠すユーティリティ
  クラス）のプレーンテキスト`<h1>`を設置。デザイン上の見出し
  （`.heroTitle`/`.pageTitle`）は`<h2>`に変更。
- `blogmain.html`：force.css解析の結果、**ブログ専用の「記事タイトル」ブロックは
  `.rdarticleTitle h3`として出力される（h1ではない）**ことが判明したため、
  `<h3 class="articleTitle">`に修正。合わせて他ページ同様、body先頭に
  隠しh1を設置。
- `normalpage.html`は複数ページで使い回すテンプレートのため、隠しh1の文言も
  ページごとに化ツール側でH1指定をやり直す必要がある（「対象ページ選択」は
  ページ単位の作業のため）。現状は「賃貸物件一覧」用の文言をサンプルとして
  入れている。
- `blogframe.html`（ブログ一覧・システムが動的に生成するページ）については、
  通常ページと同じ「5.H1部分の指定」の対象になるか**未検証**。化ツール上で
  blogframe.htmlが選択可能ページとして出るか確認が必要（10節「要検証事項」
  参照）。

`.visuallyHidden`ユーティリティクラス（`common/css/style.css`冒頭に定義済み）：

```css
.visuallyHidden{
  position:absolute;width:1px;height:1px;padding:0;margin:-1px;
  overflow:hidden;clip:rect(0,0,0,0);white-space:nowrap;border:0;
}
```

## 6. エリア・ブロックの詳細仕様

- **横並びエリア**は`.rdflexarea`（`display:flex`）、**折り返しエリア**は
  `.rdhorizontalarea`（`display:flex;flex-wrap:wrap`）として実際に出力される
  （force.css実物で確認済み）。576px以下では、`data-flexareawrap="1"`が
  付いていない限り、各子要素が`flex:1 1 100%;width:100% !important;`で
  縦積みに強制される。
- **エリアの列数（横並び／折り返しの段組み）はWebChanger側のUI設定で制御される
  ものであり、こちら側のCSS（`grid-template-columns`等）で直接指定するもの
  ではない**、という理解が重要。`taisei-site/`の`.recoGrid`
  （`display:grid;grid-template-columns:repeat(3,1fr)`）のような自前グリッドは、
  **エリアのコンテナ自体（`{%%rdarea%%}`タグの外側にある`<div class="recoGrid">`）
  としてテンプレートに書く分には本番出力でそのまま機能する**が、これを
  「WebChangerの横並び／折り返しエリア機能」で組みたい場合は、列数指定は
  ツールのUI側で行うものであり、こちらのCSSのgrid-template-columns指定は
  参考にならない（＝実際の列数制御はWebChanger側の管理下に移る）。
  **✅2026-08-18、WebChanger公式の標準ブロック仕様書で正確な制御方法が判明した
  （13節D参照、10節6番も解決）**：横並びエリア（`colmode="1"`）は配下の
  単体エリアの「個数」がそのまま列数になり、幅は均等割り＋インラインstyleで
  個別出力される（ドラッグでの再配分も可能）。折り返しエリア（`flexmode="1"`）は
  配置時に列数を明示指定する専用編集UIがあり、幅はシステム発行のクラス名で
  制御される。いずれにせよ「こちらのCSSで列数を直接指定することはできない」
  という結論は変わらない。
- ブロックの装飾機能（枠線・背景色・角丸・box-shadow・margin/padding/width等）
  は「標準CSS」として自動生成される。カスタムCSSはその後（`/*$$$*/`区切り
  以降）に追記する形になり、同一プロパティが両方にある場合は後（カスタム側）が
  優先される。
- **【うにさんの実務パターン・2026-08-18追記】トップページ以外のテンプレートには
  パンくずリスト用のエリアを作ることが多い**。トップページ（`toppage.html`）は
  サイトの起点なのでパンくずが不要だが、`normalpage.html`・`blogframe.html`・
  `blogmain.html`のような他ページでは、標準ブロックの「パンくずリスト」ブロック
  （`.rdBreadcrumb`、9節・11節`pan.html`参照）を配置する専用エリアを
  ヘッダー直後あたりに用意するのが通例。ページごとに内容が異なる（会社案内なら
  「トップ＞会社案内」等）ため、共通エリア（`cmn`）ではなく通常エリア（`are`）が
  適切。`taisei-site/`の該当テンプレートにも候補エリアとして反映した（下記参照）。
- **【うにさんの実務パターン・2026-08-18追記】メインエリアの上下に、リリース後の
  保険として空の共通エリア＋通常エリアを置くこともある**（デザインによる）。
  サイト公開後にクライアントから「メインコンテンツの上/下にもう1つセクションを
  追加したい」という要望が来た際、テンプレート自体を作り直さずに済むよう、
  あらかじめ空のエリア（共通エリア1つ＋通常エリア1つのペア、など）を仕込んで
  おく、という保険的な設計パターン。空の状態では見た目に何も影響しない。
  `taisei-site/`の各テンプレートのメインコンテンツ（`mstcon="1"`候補）の直前・
  直後にも候補コメントとして反映した（下記参照）。

## 7. ブログ関連の仕様

- **ブログフレーム用HTML（`blogframe.html`）にはエリアを置けない**。
  設置できるのは共通エリア／ブログ共通エリア／ブログフレームタグ
  （`{%%rdblogframe%%}`、システム生成の一覧・検索結果を出力する）のみ。
  → `blogframe.html`の`.blogGrid`〜`.blogPager`は**あくまで見た目確認用の
  モックアップ**。実際の変換時はこのブロックごと`{%%rdblogframe%%}`に
  置き換わる。
- **実際のブログ最新記事一覧ブロックの出力構造は`.rdnewArticle`系クラス**：

  ```html
  <ul class="rdnewArticle">
    <li class="rdnewArticle-item">
      <div class="rdnewArticle-thumbnail">
        <a class="rdnewArticle-ContLink rdnewArticle-ContLink-thumbnail">
          <img class="rdnewArticle-thumbnail-image">
        </a>
      </div>
      <div class="rdnewArticle-contWrap">
        <div class="rdnewArticle-ContDate">日付</div>
        <div class="rdnewArticle-ContTitle">
          <a class="rdnewArticle-ContLink">タイトル</a>
        </div>
        <div class="rdnewArticle-ContEx">
          <ul class="rdnewArticle-catlist">...</ul>
          <ul class="rdnewArticle-taglist">...</ul>
        </div>
      </div>
    </li>
  </ul>
  ```

  **サムネイル・カテゴリ/タグは標準では`display:none`**（force.css既定値）。
  表示させるには明示的に`display:block`等で解除するCSSが必要
  （公式リリースノート記載の既知仕様）。`common/css/style.css`には既に
  `.rdnewArticle`系のブランドカラー対応スタイルを追記済み（デフォルトの
  非表示解除も含む）。
- **ブログ記事タイトルブロックは`.rdarticleTitle h3`として出力される**
  （h1ではなくh3。5節参照）。**公開日は`.rdpublishDate`クラス**として
  出力される。
- **ブログ記事タイトルの詳細度の罠**：force.cssの`.cmshtml .rdarticleTitle h3`は
  クラス2つ+タグという高い詳細度を持つ。自分のCSS
  （`.articleTitle{...}`、クラス1つ）は変換後`.cmshtml`配下に自動的に
  移されるため詳細度が同点になり、**読み込み順（force.cssが後）で
  force.css側が勝ってしまう可能性がある**。そのため
  `common/css/style.css`には`!important`付きの上書きルール
  （`.rdarticleTitle h3{...!important}`）と`.rdpublishDate{...!important}`を
  追加済み。同様の考え方は、見出しブロック全般（`.rdplanetitle h2`〜`h6`、
  force.css既定でh2=3remなど）にも適用し、ブランドに合わせた上書き
  （`!important`付き）を追加済み。
- ブログ記事URLは実例（mizuhokai.or.jp調査より）ではタイムスタンプ状の
  数値ID（例：`/news_top/1785128509`）で自動生成される。日本語スラッグ等には
  ならない前提でよい。
- カテゴリ絞り込みはクエリパラメータ形式（例：`/news_top?cate=1`）で
  実装される（同上の実例調査より）。

## 8. 内容変更画面（編集画面）特有の見え方の注意（新発見・最重要）

**この節が最新の発見であり、最も実務的に重要**。「HTML・CSS修正事例」という
社内資料（WebChangerの標準ブロック資料の一部、URLの末尾は`edit.html`）を
確認した結果判明した。

**大前提**：以下の問題は**すべて「WebChangerの内容変更画面（CMS編集UI）」限定の
問題であり、実際に公開されるサイトの見た目には影響しない**。編集作業をする
スタッフの使い勝手の問題として捉えること。

- **内容変更画面では、各ブロックが編集用に`.rdareaItem`→`.rdareaContents`という
  2重のdivで追加ラップされる**。この追加ラップにより、DOM階層が本番出力より
  2段深くなる。これが以下の諸問題の根本原因。

  ```
  本番出力：<div class="module">...実際のブロック内容...</div>
  編集画面：<div class="rdareaItem">
              <div class="rdareaContents">
                <div class="module rditemGeenerated">
                  ...実際のブロック内容（さらに入れ子）...
                </div>
              </div>
            </div>
  ```

- **基本的な考え方（重要な方針転換）**：内容変更画面では階層が変わるため、
  **各ブロックのクラスに対して直接スタイルを指定するのではなく、親クラスから
  指定すると内容変更画面でのカスタマイズが容易になる**。
  「旧WebChangerとは逆の考え方となる点を注意」と資料に明記されている
  （つまり、子要素セレクタではなく、親要素起点のセレクタ設計を意識すべき）。

- **テンプレート側が読み込む外部JSファイル（force.jsや`common/js/script.js`など）は
  内容変更画面では一切実行されない**（ブラウザでJavaScript無効にした状態と同等）。
  **ただし2026-08-18に判明した精度上の補足（13節H参照）**：WebChanger自身が
  提供する標準ブロック（画像スライドショー等）は、force.jsとは別に編集画面専用の
  内製スクリプトで同等の動作を独自に再現しており、見た目上は動いて見える場合が
  ある。この例外はWebChanger標準ブロックに限られ、`taisei-site/`が独自実装している
  ヒーロースライダー・`.reveal`・ナビトグルなどのカスタムJSには適用されない
  （＝以下の対処方針はそのまま有効）。これにより：
  - `taisei-site/`独自実装のスライドショー等、動的な要素はそのまま（初期状態）表示される。
  - **`opacity:0`や`display:none`を初期値とするCSS（スクロール演出等）は、
    JSが動かないため永久に解除されず、編集画面上でコンテンツが見えなくなる**。
    → `taisei-site/`の`.reveal`クラス（IntersectionObserverでスクロール
    フェードインさせる自前の演出）がまさにこれに該当する問題だった。
    対処として`common/css/style.css`に以下を追記済み：
    ```css
    #editView .reveal{opacity:1 !important;transform:none !important;}
    ```
    今後同様のJS依存の初期非表示パターンを追加する場合は、必ず
    `#editView`配下での強制表示CSSをセットで書くこと。
  - `taisei-site/`のヒーロースライダー（`#heroSlider`）はJS
    （`common/js/script.js`）で1枚目に`isActive`クラスを付与しているが、
    **HTML側で最初から`class="slide isActive"`をハードコードしている**ため、
    JSが動かなくても1枚目は正しく表示される（この点は元々安全設計だった）。
    ただしドット（`.heroDots`の中身）はJSで動的生成しているため、
    編集画面ではドットが一切表示されない点は許容範囲として認識しておくこと。
  - モバイル用ナビの開閉（`#navToggle`によるクラストグル）も同様にJS依存。
    編集画面が仮にモバイル幅でレンダリングされた場合、ナビが開閉できない
    状態で固定表示される可能性がある。**未対応・要検討事項**。

- **`opacity:0`/`display:none`系の対処パターン（一般形）**：

  ```css
  /* 元々のCSS（本番用） */
  .cmshtml .section_carousel { opacity: 0; transform: translateY(50px); }

  /* 追加：編集画面だけ強制的に見えるようにする */
  #editView .cmshtml .section_carousel { opacity: 1; transform: none; }
  ```

  「常時表示」にするか、WebChangerの「非表示要素を表示」オプション
  （`#editView.showhideElement`というクラスが本体に付与される）が
  有効なときだけ表示にするか、を状況に応じて判断する。

- **`position: absolute`は、編集画面の追加ラップdivが`position:relative`に
  なるため、絶対配置していた要素の位置が本来の意図からずれる**。
  厳密に同じ位置を再現するのは難しいため、多くの場合`static`に変更して
  対応する：
  ```css
  .abs { position: absolute; left: 100px; top: 50px; }
  #editView .abs { position: static; }
  ```
  「編集画面と実際の表示が異なる場合がある」「編集時の利便性を考え、
  内容変更画面でのレイアウトを調整している場合がある」という点は
  **顧客への事前説明が必要**、と資料に明記されている。
  → `taisei-site/`内で`position:absolute`を使っている箇所
  （`.propTag`バッジ、`.heroScrim`、`.heroDots`等）のうち、実際に
  エリア・ブロック化する予定の箇所（特に`.recoCard`内の`.recoTag`相当）は
  要注意。現状は本番用CSSのみで、`#editView`対応は**未着手**
  （10節「要検証事項」参照）。

- **`position: fixed`は編集画面の外へ要素が飛び出してしまうため、
  `absolute`か`static`に変更する**：
  ```css
  #globalHeader { position: fixed; width: 100%; padding: 20px; }
  #editView #globalHeader { position: static; }
  ```
  → **訂正**：当初`taisei-site/`は`position:fixed`を使用していない
  （`sticky`のみ使用）としていたが、`sample-project/`比較作業に伴う再点検で、
  980px以下（`taisei-site/`独自のモバイルメニュー展開ブレークポイント。
  WebChanger本体の576pxとは別軸、`taisei-site-プロジェクト情報.md`3.4節参照）で`#globalNav{position:fixed;...}`を
  実際に使用していることが判明した（10節3・5番、12節、`taisei-site-プロジェクト情報.md`7章参照）。
  **✅対応済み（2026-08-18）**：`#editView #globalNav{position:static;
  transform:none;}`を追加した。`transform`も併せて解除しているのは、
  `translateX(100%)`（閉じた状態）のままだとJSが動かない編集画面で永久に
  画面外へ隠れたままになり、10節5番の「モバイルナビが編集画面で開閉不能に
  なる」問題も同時に解消できるため（`.isOpen`を待たず常に表示される）。
  `sticky`についてはこの資料内で言及がなく、問題の有無は未検証。

- **floatは極力避ける**。ブロック表示枠やバッジの位置がずれる原因になる。
  画像右・文字左のようなカラム型レイアウトはflexで組み、内容変更時の
  表示調整をCSSで別途行う。
  → `taisei-site/`はfloatを一切使用しておらず、この点は問題なし。

- **`display:flex`の親に対し、内容変更画面で子要素の直下に追加divが挟まる
  ため、`nth-child`等で指定していたflexプロパティが効かなくなる**：

  ```css
  .module { display: flex; }
  /* 追加：編集画面用に、実際にflexが効く階層（追加divの直下）にも指定 */
  #editView .module > div:nth-child(1),
  .fl-right { flex: 0 0 auto; }
  #editView .module > div:nth-child(2),
  .text { order: 1; flex: 1 1 auto; }
  ```

  ただしこの方法は「並び替えるとレイアウトがおかしくなるおそれがある」ため、
  **横並びレイアウトは、可能な限りWebChanger標準の横並びエリア機能の使用を
  推奨**、と資料に明記されている（6節の指摘と一致）。

- **`display:inline`/`inline-block`で横並びにしていたdivが、編集画面では
  ブロックが`div`で囲まれるため縦並びになる**（`display:block`になって
  しまうため）。親要素からのセレクタで`display:inline`を再現する：
  ```css
  #editView .footerlink > div,
  .inline { display: inline; }
  ```

- **階層セレクタ（`div > div`）・隣接セレクタ（`div + div`）は、編集画面で
  DOM階層が変わるため効かなくなる**。既存CSSに`#editView`配下のみ効く
  形で追記して対応する：
  ```css
  #editView .mainnav div div a,
  .cmshtml .mainnav ul a { color: #222; padding: 40px 0; display: block; }
  ```
  → `taisei-site/`のヘッダーナビ（`.navUl > li > div > a`等）は、
  **ナビ自体をエリア・ブロック化しない固定テンプレート要素として扱う想定**
  のため、このリスクは現状回避できている（ブロック化された場合のみ発生する
  問題のため）。

## 9. force.css / force.js の実クラス一覧（抜粋・要点）

WebChangerが常に読み込む固定CSS/JS（`{%%rdtplcmn_fix%%}/force.css`、
`force.js`）から確認できた実際のクラス名・仕組み。今後、標準ブロックの見た目を
上書きしたいときはこれらのクラス名をターゲットにする。

**汎用**
- `.cmshtml { --rdvw: 1vw; }` … JS側で`document.documentElement.clientWidth/100`
  から動的に再計算されるカスタムプロパティ（メガメニューの幅計算等で使用）
- `.rdflexarea` / `.rdhorizontalarea` … 横並び／折り返しエリアの実体
  （6節参照）
- `[data-rdhide="pc"]` / `[data-rdhide="sp"]` … PC/SP出し分け
  （ブレークポイント576px、4節参照）

**見出し・テキスト**
- `.rdplanetitle h2〜h6` … 見出しブロックの既定サイズ（h2=3rem等）。
  ブランド上書き必須（7節参照、実施済み）
- `.rdplanetext` / `.rdplanetext2` … テキストブロックの表組み等

**画像・地図・YouTube**
- `.rdplaneimage img` … 画像ブロック
- `.rdplaneyoutube` / `.rdplanemap` … padding-top:56.25%のアスペクト比
  トリックで実装。`#editView`配下では別スタイル
  （height:auto等）になるよう既に配慮されている（K-010で確認した
  「編集する」ボタンの位置ズレ問題の根本原因もここ）

**ブログ関連**
- `.rdcomment` … コメント機能
- `.rdListItem` / `.rdListResult` / `.rdListPageResult` … 一覧・検索結果
- `.rdnewArticle` … 新着記事一覧（7節で詳述）
- `.rdCalendar` / `.rdCategorylist` / `.rdTaglist` / `.rdMonthlyArchive` /
  `.rdYearlyArchive` … ブログ周辺機能一式
- `.rdarticleTitle h3` / `.rdpublishDate` … 記事タイトル・公開日
  （5節・7節で詳述）
- `.rdBlogSearch` / `.rdSiteSearch` … 検索フォーム（jQuery側で2文字未満は
  alertで弾く実装）
- `.rdRelatedArticles` … 関連記事

**フッター・サイトマップ**
- `.rdsitemapft` … フッターリンク自動生成機能（階層構造対応）
- `.rdSitemap_pg` … サイトマップページ
- `.rdBreadcrumb` … パンくずリスト

**お問い合わせフォーム**
- `.contact_item_table` / `.contact_item_title` / `.contact_item_input` …
  基本構造
- `.contact_item_namebox_box` … 姓名分割フィールド
- `.contact_item_inside_table` … 住所等のネスト項目
- `.contact_sepalate_zip` / `.contact_tel_sepalate` … 郵便番号・電話番号の
  分割入力
- `.contact_radio_box` / `.contact_checkbox_box` … ラジオ・チェックボックス
- `.rdcontact_btn_area` / `.contact_confirm_msg` … 送信ボタン・確認画面
  → **`taisei-site/`にはまだお問い合わせページ自体が存在しない**。
  将来作成する際は、独自マークアップではなくこの実クラスに準拠して
  装飾すること。

**表（テーブル）ブロック**
- `.rdtable` / `.rdtableTable` … 基本
- `.rdtable-pattern01`〜`04` … 装飾パターン（枠なし下線のみ／セル間仕切り線
  付き／全枠線＋ヘッダー背景／完全装飾なし）
  → 会社概要ページ等で表組み（免許情報等）を作る際に使える

**スクロールアニメーション**
- `[data-rdaos]`, `data-rdaos-duration`, `data-rdaos-delay`,
  `data-rdaos-easing` … WebChanger標準のAOS風スクロール演出機構
  （RDAOSという内製ライブラリ、実体はaos.jsのフォーク）。
  `.rdaos-animate`クラスの有無で表示・非表示を切り替える。
  **`window`の`load`イベントで初期化される**（`DOMContentLoaded`ではない）。
  `taisei-site/`の`.reveal`（独自IntersectionObserver実装）とは別物であり、
  対象クラスが異なるため直接の衝突リスクは低いと判断済み。
  ただし将来、コンテンツ編集者がWebChanger標準の`data-rdaos`属性を独自に
  追加した場合、うちの`.reveal`と二重に演出がかかる可能性はゼロではない。

**ナビゲーションブロック**
- `.rdNavigation-horizontal01`〜`03` / `.rdNavigation-vertical01`〜`03` /
  `.rdNavigation-button01` / `.rdNavigation-mega01` … 各種ナビゲーション
  デザインパターン。`--rdNaviBgColor` / `--rdNaviTextColor`という
  CSSカスタムプロパティで配色を制御する仕組み。
  576px以下でのハンバーガーメニュー開閉はforce.js側の
  `naviBtnToggle`/`handleTabletChange`/`megaFix`関数が処理。
  → `taisei-site/`の現行ヘッダーナビは**この標準ブロックを使わず、
  独自の固定HTMLとして実装している**（会社の看板・ロゴに近い扱いで、
  頻繁に編集する想定がないため）。将来、クライアント側でメニュー構成を
  自由に編集できるようにしたい場合は、この標準ナビゲーションブロックへの
  置き換えが選択肢になる。

**画像スライドショーの正体**
- **`.rdslide`系クラス（`rdsp-*`）は、`slider-pro`（bqworks製、v1.6.0）
  というjQueryプラグインをそのまま内製化したもの**。
  `data-type`属性で種類を切り替える：
  - `data-type="1"`：通常のスライド（左右にスライドして切り替え）
  - `data-type="2"`：フェード切り替え
  - `data-type="3"`：無限ループ（`rdinfiniteslide`、横に流れ続けるタイプ）
  その他の制御属性：`data-height`・`data-width`・`data-gap`・`data-speed`・
  `data-interval`・`data-direction`・`data-easing`・`data-variableheight`
  → 元の`taisei-kougyou.net`旧サイトも`jquery.sliderPro.js`を使っていた
  ため、技術的な系譜が繋がっている。
  → **`taisei-site/`のヒーロー画像は、あえてこの標準スライドショーブロックを
  使わず、単一画像＋「ブロックへの背景画像設定機能」で実装することを推奨**
  としている（ガイド`taisei-site-エリアブロック設置ガイド.md`参照）。
  理由：標準スライドショーブロックの出力クラス（`.rdslide`/`.rdsp-*`）は
  今回のデザイン（フルブリード＋グラデーションスクリム＋独自ドットスタイル）
  と噛み合わせるための追加CSSが必要になり複雑なため。ただし技術的には
  対応可能（クラス名は判明済み）なので、複数枚の切り替えにどうしても
  こだわりたい場合は`.rdslide`系クラスへのブランドCSS追加も選択肢としてある。

**画像ライトボックス（ポップアップ拡大表示）の正体**
- **`lightbox2`（Lokesh Dhakar氏作、v2.11.2）を`rd`プレフィックスで
  内製化したもの**。トリガーは`<a target="rdlb" href="拡大画像パス">`
  （通常の`rel="lightbox"`ではない点に注意）。同じ`data-rdlightbox`属性値を
  持つ複数画像をグループ化すると、ギャラリー形式（前へ／次へ送り）になる。
  → 将来、物件写真ギャラリーページを作る際に使える。`taisei-site/`では
  まだ未使用。

## 10. 未確認・要検証事項（Claude Codeへの申し送り）

以下は、資料上明記がない、または実際にWebChanger化ツールを動かして確認しないと
分からない事項。次の作業者（Claude Code）が実際にツールに触れる、または
追加資料が提供された際に検証・反映すること。

1. ~~**`body{...}`セレクタが自動的に`.cmsbody`へ変換されるか**~~
   → **✅解決済み（12節参照）**：`sample-project/`の実物比較で確認。
   `reset.css`の`body{line-height:1;}`が変換後`.cmsbody{line-height:1;}`に
   自動変換されていた。手動で`.cmsbody`に直す必要はなく、`body{...}`のまま
   書いてよい（ただし後述の通り`.cmshtml`/`.cmsbody`クラス自体は変換前から
   `<html>`/`<body>`に手書きしておく必要がある。これは別の話）。
2. ~~**`blogframe.html`が「5.H1部分の指定」の対象ページとして選択できるか**~~
   → **△ほぼ解決（12節参照）**：`sample-project/02OutData_comp/tplcust/blogframe.html`
   の出力を確認したところ、`blogframe.html`にも通常ページと全く同じ
   `<h1 class="globalh1">{%%rdtplblk_h1%%}</h1>`が生成されていた。テンプレート
   側の仕組みとしては通常ページと同格にH1プレースホルダーを持つことが確認できた
   （＝現状の`visuallyHidden`のh1設置方針のままでよい）。ただし化ツールの
   GUI操作（「対象ページ選択」画面で実際に選べるか）そのものは、静的サンプルの
   比較だけでは検証しきれないため、実機確認は引き続き推奨。
3. **`position:sticky`は内容変更画面で問題を起こすか**：
   「HTML・CSS修正事例」資料では`fixed`と`absolute`への言及はあるが
   `sticky`への言及がない。`#globalHeader`・`#contactBar`で`sticky`を
   使用しているため、実際に編集画面で確認が必要（未検証のまま）。
   なお`#globalNav`（モバイルメニュー展開時）の`position:fixed`については
   ✅対応済み（下記・`taisei-site-プロジェクト情報.md`7章参照）。`sticky`単体の検証課題のみ残っている。
4. **`.propTag`（物件カードの「賃貸」等のバッジ、`position:absolute`）が
   編集画面でズレるか**：これがユーザーブロック化された場合、
   `.rdareaItem`/`.rdareaContents`の追加ラップの影響で位置がズレる
   可能性が高い。`#editView`向けの`position:static`フォールバックを
   追加すべきか検討要。**未対応**（`.heroDots`・`.heroScrim`・`.navToggle span`等
   他のposition:absolute箇所も同様に要点検、12節I参照）。
5. ~~**モバイルナビ（`#navToggle`によるJS制御）の編集画面での挙動**~~
   → **✅対応済み（2026-08-18）**：`#globalNav`の`position:fixed`に
   `#editView`フォールバック（`position:static;transform:none;`）を追加した際に、
   `transform`も同時に解除したため、JSが動かない編集画面でも`#globalNav`が
   常に表示される状態になり、この問題も合わせて解消した（8節・`taisei-site-プロジェクト情報.md`7章参照）。
6. ~~**横並び／折り返しエリアの列数制御の実際の挙動**~~
   → **✅解決済み（13節D参照）**：WebChanger公式の標準ブロック仕様書で確認。
   横並びエリア（`colmode="1"`）は配下の単体エリアの個数＝列数、幅は均等割り＋
   インラインstyle。折り返しエリア（`flexmode="1"`）は明示的な列数指定UIがあり、
   幅はシステム発行クラス名で制御。いずれも自前の`grid-template-columns`は列制御に
   一切関与しない。`taisei-site-エリアブロック設置ガイド.md`の見直し時にこの仕組みを
   反映すること。
7. ~~**リンクの編集キー（`{%%編集キー%%}`）の正確な書式**~~
   → **✅ほぼ解決（12節E・13節B/G参照）**：WebChanger公式資料で
   **リンク専用の編集キーは存在しない**ことが確定した。「単独の`<a>`要素」は
   `sample-project/`の実例通りテキストブロック（`edtxt_N`）にリンクごと吸収される。
   リッチテキスト内のリンクは、サイト内・ファイルリンクの場合は編集画面で
   `<a href="ページ名称" data-rdhref="{url@ページID}">`という形式を取り、
   出力時に`data-rdhref`が削除されhrefが実URLに置換される（外部リンクは単純に
   `<a href="URL">`）。画像に紐づくリンク（`<a><img></a>`）は画像ブロック自体の
   「リンク設定」機能に吸収される（12節G）。ナビゲーション（`<a>`が構造の一部の
   場合）は個別のユーザーブロックとして作り込む前提であり、専用の「リンク編集キー」
   という形では提供されない、という結論でよい。
8. **`.cur`カーソル画像ファイルは`common/img`変換時にどう扱われるか**：
   12節Iの通り`sample-project/`では変換時に`.cur`ファイルが運ばれていなかった
   （非対応拡張子の可能性）。`taisei-site/`では現状`.cur`を使用していないため
   実害はないが、将来ドラッグ操作系のUIを追加する場合は要注意。
9. ~~**カード系コンポーネント（`.recoCard`・`.rentCard`等）は現状の設計のままでは
   カスタムブロック化できない**~~
   → **✅解決済み（13節A-1参照）**：画像・物件名・価格・所在地など複数の
   編集可能項目を持つ現行のカードHTMLは、1個のカスタムブロックとしては登録
   できないことが判明（13節A）。当初は「カード外枠を`are911`の標準エリアに
   したカスタムブロック」案を検討していたが、うにさんの指摘で
   **「カスタムブロックを使わず、折り返しエリア＋単体エリア＋標準ブロックの
   組み合わせだけで組み、見た目はクラス追加機能で調整する」**方針に修正した
   （詳細は13節A-1）。下記10番も解決済みのため、この方針で問題なく成立する。
   `taisei-site-エリアブロック設置ガイド.md`の見直し（`taisei-site-プロジェクト情報.md`8章2番）にこの新方針を
   反映すること。
10. ~~**エリア（単体エリア・折り返しエリア）自体に「クラス追加機能」
    「ブロック装飾機能」が使えるか**~~
    → **✅解決済み（うにさんに確認・2026-08-18）**：**メインエリア（`mstcon="1"`の
    大外の枠）には使えないが、その配下のエリア（子エリア・単体エリア）には
    使える**。今回の「折り返しエリア＋単体エリア（カード1枚）」という構成では、
    メインエリアそのものではなく配下の単体エリアにクラスを付与する想定のため、
    9番のA-1方針（クラス追加機能でカードの枠線・padding等を持たせる）は
    問題なく成立する。

## 11. 参照した情報源一覧

Claude Codeが追加で参照すべき、まだ見ていない可能性のある資料（すべて
「HTML・CSS修正事例」ページのサイドバーから存在が確認できたもの。
URLパスはこの資料が置かれているのと同じディレクトリ構成と推測される）：

**2026-08-18追記**：うにさんからこのリストに相当する公式資料が全56ファイル・
3回に分けて提供されることになり、1回目（44ファイル）を受領・精読済み
（内容は13節に反映）。1回目で読了確認できたもの：`base.html`（基本エリア）・
`block.html`（ブロック概要）・`def.html`（標準ブロック）・`editor.html`
（テキスト編集）・`table.html`（tableの制約）・`hide.html`（ブロックの表示・
非表示）・`class.html`（クラス追加機能）・`animation.html`（該当ファイルは
未精読だが同名の「スクロールアニメーション.txt」は受領済み、次回精読）・
`blocklink.html`（ブロックリンク）・`area.html`・`area2.html`・`area3.html`・
`text.html`・`hx.html`・`tableblock.html`・`map.html`・`youtube.html`・
`pan.html`・`news.html`・`search.html`・`contact.html`（受領済み、未精読）・
`shopping.html`・`footer.html`・`rss.html`・`anchor.html`・SNS4種・
ブログ用ブロック11種（ファイル自体は受領済み、内容の精読は次回以降）・
`user.html`（カスタムブロック）・`force.html`（force.css全文、既存9節との
再照合は次回）・`example.html`（ブロック見本、未精読）。2回目・3回目の到着後、
このリストと13節を随時更新していくこと。

**基本**
- `base.html`（基本エリア）
- `block.html`（ブロック概要）※**未読・優先度高**
- `def.html`（標準ブロック）※**未読・優先度高**
- `editor.html`（テキスト編集）
- `table.html`（tableの制約）※UPDATE表記あり
- `hide.html`（ブロックの表示・非表示）
- `class.html`（クラス追加機能）※`taisei-site/`のCSS設計に直結、優先度高
- `animation.html`（スクロールアニメーション）※`data-rdaos`の詳細
- `blocklink.html`（ブロックリンク）※`rdhaslink`の詳細

**エリア**
- `area.html`（エリア）※**未読・優先度高**
- `area2.html`（エリア（横並び））※10節の疑問に直結、優先度高
- `area3.html`（エリア（折返し））

**コンテンツブロック**
- `text.html`（テキスト）
- `text2.html`（2段：表題+本文）
- `image.html`（画像）
- `slide.html`（画像スライド）※`.rdslide`の公式仕様
- `tableblock.html`（表）※NEW表記あり

**見出し**
- `hx.html`（見出しh2〜h6）

**特殊ブロック**
- `map.html`（地図）
- `youtube.html`（YouTube）
- `pan.html`（パンくずリスト）
- `news.html`（新着情報・簡易）
- `search.html`（サイト内検索）
- `contact.html`（お問い合わせ表示枠）
- `shopping.html`（ショッピング表示枠）
- `footer.html`（フッタマップ）
- `rss.html`（RSSフィード）
- `anchor.html`（アンカー）
- `instagram.html` / `twitter.html` / `facebook.html` / `tiktok.html`
  （各SNS埋め込み、NEW表記あり）

**ブログ用ブロック**
- `blogdate.html`（公開日時）
- `blogtitle.html`（タイトル）※5節の裏付け資料
- `blogrelation.html`（関連記事）
- `blogtoc.html`（目次）
- `bloglist.html`（ブログ最新記事一覧）※7節の裏付け資料
- `blogsearch.html`（ブログ内検索）
- `blogcal.html`（ブログカレンダー）
- `blogmonth.html`（ブログ月別アーカイブ）
- `blogyear.html`（ブログ年別アーカイブ、NEW表記あり）
- `blogcat.html`（ブログカテゴリー一覧）
- `blogtag.html`（ブログタグ一覧）

**カスタムブロック**
- `user.html`（カスタムブロック）※ユーザーブロックの正式仕様、優先度高

**参考資料**
- `force.html`（force.css全文）※既に別途提供され読了済み（本ドキュメント9節に反映）
- `example.html`（ブロック見本）

**外部サイト調査（Claude自身がWeb検索で確認したもの）**
- GlobalGate公式サイトのWebChanger紹介ページ
  （`https://www.globalgate.co.jp/webchanger`、機能一覧）
- 同社の動画埋め込み特設ページ（`https://www.globalgate.co.jp/webchangerlp101`）
  ※YouTube動画2本（第1章：文章や画像の変更、第2章：エリアとブロックのコピー）
  が埋め込まれていることを確認。動画自体の中身は未視聴（視聴推奨、特に
  第2章はエリア・ブロック作業と直結）
- 実際に稼働中のWebChangerサイト`mizuhokai.or.jp`（医療法人瑞穂会）を
  外部から調査し、クリーンURL（`.html`が付かない）・ブログ記事URLの
  数値ID形式・カテゴリのクエリパラメータ形式・サブディレクトリでの
  複数サイト運用（`/gh-mizuho/`等）・リニューアル時の告知記事パターンなどを
  確認（※HTMLソース自体は取得できず、Markdown変換後の内容からの推測を
  含む点に留意）

## 12. `sample-project/`実物比較で判明した追加知見（2026-08-18・最重要）

`01MstData/`（変換前）と`02OutData_comp/`（変換後）を実際にファイル単位で
diffして判明した、資料からは読み取れなかった実挙動。すべて実ファイルの
一次証拠がある内容なので、本ガイドの他の記述と食い違う場合はこちらを優先すること。

**A. `<html class="cmshtml">`・`<body class="cmsbody">`は変換前から手書きする**
`01MstData/`の全HTMLがすでにこの2クラスを持った状態で書かれている
（＝ツールが自動付与するのではなく、コーディング規約として最初から書く）。
3節の記述を確認済みに更新済み。**`taisei-site/`の4テンプレート全てに追加済み
（2026-08-18対応）**（`taisei-site-プロジェクト情報.md`7章参照）。

**B. `body{...}`セレクタは自動的に`.cmsbody{...}`に変換される（確認済み）**
`reset.css`の`body{line-height:1;}`が変換後`.cmsbody{line-height:1;}`になっていた。
`html,body,div,...`のようなカンマ区切りの一括セレクタでは、`html`→`.cmshtml`、
`body`→`.cmsbody`（どちらも裸のクラス、`.cmshtml `のような子孫セレクタ接頭辞
ではない）、それ以外のタグ→`.cmshtml タグ名`という規則で変換される。

**C. `.cmshtml`プレフィックス変換は自作CSSだけでなく`common/css`配下の
全CSSファイル（サードパーティ/ベンダーCSS含む）が対象**
スライダーライブラリ`slider-pro.css`のような外部プラグインCSSも同様に
全セレクタへ変換がかかっていた。「自分の書いたCSSだけ気をつければよい」
という認識は誤りで、`common/css`に置く時点で全ファイルが変換パイプラインを
通る前提で考えること。

**D. `<title>`タグは変換時に破棄され、`{%%rdtplblk_head%%}`という
システム管理プレースホルダーに統合される**
テンプレートの`<title>株式会社サンプル商店</title>`は変換後跡形もなく消え、
`<head>`内は`<meta charset...>`の直後に`{%%rdtplblk_head%%}`が挿入されるのみ
だった。ページタイトルやメタ情報はCMS管理画面側で個別管理される前提であり、
**テンプレートHTMLの`<title>`の中身に凝る必要はない**（開発時のブラウザタブ
確認用としてダミーの`<title>`を残しておくこと自体は問題ない）。

**E. 相対リンク（`../`を含むhref）は変換時に絶対URL化される**
`page/index.html`（サブディレクトリ配置）の`<a href="../#">`が、変換後
`<a href="https://example.com/#">`という絶対URL（ダミードメイン）に
書き換わっていた。一方、`index.html`（ルート）の同じ位置の`<a href="#">`は
そのまま`href="#"`で維持された。つまり**「`../`のように親ディレクトリを
跨ぐ相対パス」は絶対URL化の対象になり、単純な`#`やフラグメントのみの
リンクはそのまま**、という違いがある。変換後は全ページが同一階層に
フラット化される仕様と整合する。`taisei-site/`は元々`../`を使わず
同一階層内のファイル名（`toppage.html`/`normalpage.html`）で参照しているため、
現状この問題には該当しないが、新しいページテンプレートを追加する際も
`../`は使わない方針を継続すること。

**F. 単独の`<a>`要素はリンクブロックではなくテキストブロックに吸収される**
`<div class="footerContactItem01"><a href="#">資料請求</a></div>`のような
「装飾目的の単独リンク」は、変換後`<div class="rdappended" rdid="edtxt_1">
{%%edtxt_1%%}</div>`というテキストブロックになっていた。つまりリンク単体を
表す専用の編集キー書式は存在せず、hrefはWebChangerのリッチテキストエディタの
リンク機能側で設定する運用になる（10節7番参照）。

**G. 画像を包む`<a>`は画像ブロック自体に吸収される**
`<h2><a href="index.html"><img src="...logo.png" alt="..."></a></h2>`
（ロゴ）は、変換後`<h2 rdid="edimg_1_1">{%%edimg_1_1%%}</h2>`となり、
`<a>`タグ自体が消えていた。画像ブロックはブロック自体にリンク設定機能を
持つため、元のHTMLに書いていた`href`の値はテンプレートには残らない
（変換後、化ツールの画像ブロック設定画面でリンク先を別途指定し直す前提と
考えられる）。

**H. 多階層メニュー（サブメニュー付きナビ）は「同一エリアIDの再帰参照」で
表現される**
`<ul class="navUl"><li><div><a>...</a></div><ul class="navUl">...</ul></li></ul>`
という入れ子構造は、変換後「`<li>`を表すエリア（例：cmn005）の中に、
`<ul class="navUl">`を表す**同一のエリアID（cmn004）が再帰的に再登場**する」
という形で表現されていた（`useronly="1"`属性付き）。さらにこのcmn004エリアは、
グローバルナビの2箇所（サブナビ`globalNavSub`・メインナビ`globalNavMain`）
からも同じIDで参照されており、「メニュー項目リスト」を表す1つのエリア定義が
複数箇所・複数階層で使い回されている。将来ナビゲーションをエリア・ブロック化
する場合の設計の参考になる（現状`taisei-site/`のヘッダーナビは意図的に
エリア化しない固定要素として扱っているため、直ちに影響はない）。

**I. 本番出力でも、エリア/ブロック化された要素の周囲に追加のラップdivが
生まれるケースがある（編集画面限定ではない）**
`style.css`の差分で、`.cmshtml #globalNavMain>.navUl>*`（`.navUl`の直下、
つまり`<li>`自身をターゲットにしていたセレクタ）が、変換後
`.cmshtml #globalNavMain>.navUl>*>*`（`<li>`のさらに1段内側）に自動修正
されていた。同様にサブメニュー側のセレクタも`>*`が1つから4つ
（`>*>*>*>*`）に深くなっていた。これは、`<li>`のような繰り返しエリア対象
要素が、**本番の公開ページ出力においてもエリア/ブロックの実体を包む
ラッパー要素が追加で挟まる**ことを示している。8節は「編集画面
（`#editView`）限定の階層変化」という前提で書かれているが、**エリア化された
繰り返し要素については本番出力でも階層が1段以上深くなり得る**、という
前提の修正が必要。対策：エリア/ブロック化する可能性のある要素に対しては、
`>`（子コンビネータ）による厳密な階層指定を避け、子孫コンビネータ
（半角スペース）や属性頼みでないクラス設計を優先する。

**J. `.cur`カーソル画像ファイルはcommon/img変換時に運ばれない**
`01MstData/common/img/`にあった`openhand.cur`・`closedhand.cur`
（`jquery.sticky-kit.js`等が参照するドラッグカーソル）が、
`02OutData_comp/common/img/`には存在しなかった。`.cur`のような非対応
拡張子は変換時に脱落する可能性が高い。カーソル画像に依存する機能を
追加する場合は要注意（`taisei-site/`は現状未使用のため実害なし）。

**K. `index.dat`の実体は単純なJSONマニフェスト（自動生成の裏付け）**
```json
{
  "cmndata":   {"type": "cmndata",  "file": "",               "block": "cmndata.blk",   "name": "※共通エリア"},
  "blogframe": {"type": "blogframe","file": "blogframe.html", "block": "blogframe.blk", "name": "ブログ枠"},
  "blogmain":  {"type": "blogmain", "file": "blogmain.html",  "block": "blogmain.blk",  "name": "ブログ記事"},
  "page":      {"type": "pagecont", "file": "page.html",      "block": "page.blk",      "name": "ページ"},
  "toppage":   {"type": "pagetop",  "file": "toppage.html",   "block": "toppage.blk",   "name": "トップページ"}
}
```
テンプレート種別ごとに`{file, block, name}`を紐づけるだけの小さなマニフェスト
であり、`taisei-site-プロジェクト情報.md`4章・本ガイド2節で述べた「`index.dat`・ブロックフォルダは手動で用意しない」
という判断が正しかったことが実物で裏付けられた。

**L. ブログ専用の第三の共通エリア種別「ブログ共通エリア」（`blogcmn="1"`）**
`blogframe.html`と`blogmain.html`の`#pagetitle`（ブログタイトル、`cmn018`）・
`.blogSide`（ブログサイドバー、`cmn020`）は、通常のサイト全体共通エリア
（`cmn`、1節）とは別に`blogcmn="1"`という属性が付いた状態で出力されていた。
`id`自体は`cmn`プレフィックスを共有しつつ、`blogcmn="1"`のフラグによって
「ブログ関連の2テンプレート（一覧・記事）間だけで共有される専用エリア」
として扱われる、という3段階目のエリア共有スコープが存在することが判明した
（ページ固有`are` ＜ ブログ内共通`blogcmn` ＜ サイト全体共通`cmn`）。

**M. `blogframe.html`のメインコンテンツは本当にエリア化できない（再確認）**
`blogframe.html`の記事一覧を表示する位置は、変換後`{%%rdblogframe%%}`
タグに直接置き換わっており、`{%%rdarea ...%%}`のような編集可能エリアには
一切なっていなかった（7節の既存記述通り、実物でも確認）。一方
`blogmain.html`（記事詳細）の同じ位置は`mstcon="1"`の通常メインエリア
（`are001`）になっていた。

**N. `wckd.wdd`はWebChangerデスクトップツールのプロジェクトファイル
（SQLite DB）であり、Claude Codeが直接編集・生成すべきものではない**
`tbl_files`（ファイルパス管理）・`tbl_pagecont`（ページ内容）・
`tbl_pagearea`（ページ内エリア構造）・`tbl_mstcmnarea`（共通エリア定義）
といったテーブルを持つ、ツール内部の管理用データベース。`taisei-site/`の
制作段階でこれに相当するファイルを作る必要はない（2節の「HTML/CSS/JS
一式だけ用意すればよい」という方針を裏付ける）。

## 13. 標準ブロック仕様書（WebChanger公式資料）精読で判明した詳細仕様（2026-08-18・第1便）

うにさんからWebChanger公式の標準ブロック仕様書一式（11節に列挙していた`block.html`
`def.html``area.html`等に相当する資料、全56ファイルを3回に分けてアップロード予定の
1回目・44ファイル）が提供された。すべて実際の管理画面HTML・出力HTML・適用CSSが
明記された一次資料であり、これまで実機未検証だった10節の項目の多くを解決する。
以下は1回目でとくに重要な発見のみ抜粋（全ファイルは`common/css/style.css`等への
反映が今後必要）。以降の資料が届き次第この節に追記していく。

**A. カスタムブロックは「1ブロック＝編集可能領域は1箇所だけ」が絶対ルール**
```html
<!-- OK：固定テキスト＋編集可能画像1つ -->
<div class="custom">
    <div>すごい画像です</div>
    <div rdid="edimg_1_1">{%%edimg_1_1%%}</div>
</div>

<!-- NG：編集可能領域（rdid）が2つ以上ある -->
<div class="wrong">
    <div>すごい画像です</div>
    <div rdid="edimg_1_1">{%%edimg_1_1%%}</div>
    <div rdid="edtxt_1">{%%edtxt_1%%}</div>
</div>
```
加えて「ブロックは1つのdiv内で完結させる」（ブロックHTMLのルート要素は1つのdivのみ）
というルールもある。**これは`taisei-site/`の設計に直接影響する重要な制約**：
`.recoCard`（画像＋物件名＋価格＋所在地の4項目が編集可能）や`.rentCard`のような
「1枚のカードに複数の編集可能項目を持つ」構成は、**1つのカスタムブロックとしては
組めない**。ただし、だからといって「カスタムブロックの中にare911エリアを仕込む」
必要があるとは限らない。実務的な設計方針はA-1参照（2026-08-18、うにさんの指摘で
方針修正）。

**A-1. 多項目カードの設計パターン（2026-08-18・うにさんの指摘で方針修正）**
当初案（カード＝カスタムブロック1つ＋内部に`are911`単体エリアを1つ埋め込む）は
撤回した。うにさんの指摘：「カスタムブロックは特殊なコードを埋め込むときくらいしか
使わない。今回のようなケースは横並びエリア（実際には折り返しエリア）を組み合わせて
作り、その後必要なクラスをつけてデザインするのではないか」。これを受けて、
`.recoCard`・`.rentCard`・`.heroCard`のような繰り返しカードコンポーネントは
**カスタムブロックを使わず、エリアの入れ子だけで組む**方針に修正した：
- カードグリッド全体（`.recoGrid`/`.rentGrid`相当）を**折り返しエリア**
  （`flexmode="1"`）とする。列数は編集画面側のUIで設定する（13節D参照）。
- 「カード1枚」＝折り返しエリア配下の**単体エリア**（通常のエリア。カスタム
  ブロックではない）とする。単体エリアはコピー・追加ができるため、これ自体が
  「カードを繰り返し追加する」操作を担う（13節D「配下エリアはコピーや追加を
  行うことができ」を参照）。`are911`という特別IDは、あくまで
  **カスタムブロックの内部にエリアを仕込みたい場合**に使うものであり、今回のように
  最初からエリアの入れ子だけで組む場合は不要（13節Cの前提を再整理）。
- カードの中身（画像・見出し・タグ・価格・所在地など）は、その単体エリアの中に
  **標準ブロック**（画像ブロック・見出しブロック・テキストブロック）を必要な数
  だけ縦に積んで作る。カスタムブロックは一切使わない。
- カードの見た目（枠線・角丸・背景・padding・box-shadow等）は、WebChangerの
  「クラス追加機能」（13節J）で単体エリア・各ブロックの最上位要素に
  `recoCard`／`.recoThumb`／`.recoBody`相当の既存CSSクラスを付与することで、
  `common/css/style.css`の既存スタイルをそのまま流用する想定。
- **CSSの当て方（うにさんの実務パターン、2026-08-18確認）**：
  「このクラスのエリアの中の、このクラスのブロックには、このCSSが効く」という
  形で使うのが基本パターン。つまり**単体エリア側に固有クラス（例：`recoCard`）を、
  各標準ブロック側にも個別クラス（例：`recoTag`／`recoPrice`／`recoMeta`）を
  それぞれクラス追加機能で付与し、CSSは`.recoCard .recoTag{...}`
  `.recoCard .recoPrice{...}`のように**エリアのクラスを起点にした子孫セレクタで
  配下のブロックへスタイルを当てる**。同じ「テキストブロック」（出力クラスは
  共通で`.rdplanetext`）を1つのエリア内で複数使う場合（recoCardのタグ／価格／
  所在地など）、個々のブロックを区別するにはこの**ブロック側の個別クラス付与が
  必須**になる（`.rdplanetext`だけでは区別できないため）。これは8節の
  「子要素セレクタではなく、親要素起点のセレクタ設計を意識すべき」という原則と
  一致する実務上の具体パターンとして採用する。
- カード全体をリンクにしたい部分は、hrefを直書きせずブロックリンク機能
  （`rdhaslink`、13節I）を使う。
- **カスタムブロックは、標準ブロック＋クラス追加機能の組み合わせでは再現できない
  「特殊なコード（inline SVGアイコンなど）を埋め込みたい」場合にのみ使う**、
  という位置づけに整理した。`.bizCell`のSVGアイコンのような固定装飾は、
  カスタムブロック化の候補になり得るが、h3見出し1つだけが編集可能なら標準の
  見出しブロックと固定SVGの組み合わせでも十分な可能性がある。
- **✅解決済み（うにさんに確認・2026-08-18）**：エリアへのクラス追加・装飾は
  **メインエリア（`mstcon="1"`の大外の枠）には使えないが、その配下のエリア
  （子エリア・単体エリア）には使える**。今回の「折り返しエリア＋単体エリア」
  という構成では、クラスを付けたい「カード」は配下の単体エリアであり
  メインエリアそのものではないため、上記の方針で問題なく成立する（10節10番）。

**B. 編集可能タグの網羅（新発見のキーを含む）**
```html
<!-- テキスト -->
<div rdid="edtxt_1">{%%edtxt_1%%}</div>
<!-- 画像（spanでもdivでも可） -->
<span rdid="edimg_1_1">{%%edimg_1_1%%}</span>
<!-- 地図（新発見） -->
<div rdid="edmap_1">{%%edmap_1%%}</div>
<!-- Youtube（新発見） -->
<div rdid="edytube_1">{%%edytube_1%%}</div>
<!-- 表（新発見、表ブロック全体） -->
<div rdid="edtable_1">{%%edtable_1%%}</div>
<!-- 画像スライド（新発見、選択枚数分のdiv.rdslideItem>imgに置換） -->
<div rdid="edslide_1_1">{%%edslide_1_1%%}</div>
```
**リンク専用の編集キーは存在しない**ことが確定した（10節7番・12節F/G参照）。

**C. `useronly="1"`の正確な用途と、`are911`という特別ID**
- `useronly="1"`は「そのエリアに標準ブロックを設置禁止し、ユーザーブロックのみ
  設置可能にする」属性。用途は主に**table/ul/ol/dlのように子要素タグが仕様上
  固定されているタグの配下**（table直下はthead/tbody/tfoot、trの配下はth/td、
  ul/olの直下はli、dlの直下はdt/ddのみ）に親エリアを設置する場合。標準ブロックは
  すべてdivとして出力されるため、divを許可しないタグの配下では不正なHTMLになって
  しまうことを避けるための機構（12節Hで発見したナビゲーションの`cmn004`エリアも
  これに該当：`<ul class="navUl">`配下なので`useronly="1"`が必須だった、という
  理由がここで裏付けられた）。タグ固定以外にも「入力内容を固定したいスライドショー
  等」の用途にも使える。
- ネストしたエリアのIDを**`are911`にすると、それは「標準エリア（単体エリア扱い）」
  になり標準ブロックを設置できる**。`are911`以外のIDを使った場合は「同一エリアキー
  を持つブロック（＝そのIDに紐づけて個別登録したユーザーブロック）」しか配下に置け
  なくなる。カード系コンポーネントをエリア・ブロック化する際の実務上のテンプレート
  として重要（Aの補足）。

**D. 単体エリア（横並び用の1つ1つ）と、横並びエリア／折り返しエリアの列数制御の
実際の仕組み（6節・10節6番の疑問を解決）**
- **横並びエリア**：`<div class="rdflexarea rdblcok">{%%rdarea id="are901" name="..."
  colmode="1" %%}</div>`のように**`colmode="1"`属性**を付ける。列数という設定項目は
  なく、**配下に単体エリアを何個ドラッグ配置したかがそのまま列数になる**。各単体
  エリアの幅は均等割り（例：2個なら50%ずつ）で**インラインstyle（`style="width:
  50%;"`）として個別出力**される。編集画面でリサイズハンドルを使って幅を手動調整
  すると、隣接エリアの幅が合計100%になるよう自動的に再計算される。
- **折り返しエリア**：`<div class="rdhorizontalarea rdblcok">{%%rdarea id="are921"
  name="..." flexmode="1" %%}</div>`のように**`flexmode="1"`属性**を付ける。
  こちらは**配置時に「列数」を明示的に指定できる専用の編集UI**（「編集する」ボタン
  →`data-editarea`属性で識別される吹き出し）が存在し、列数は後から変更可能。配下
  エリアの幅は「100/列数%」で、**インラインstyleではなくシステムが発行する固有
  クラス名**に対して設定される。配下エリアの総数が列数を超えると折り返される。
- **いずれの場合も、テンプレート側で`grid-template-columns`等を書いても列制御には
  一切関与しない**という6節の既存記述が実物資料でも裏付けられた。列数・幅の
  制御方法が横並び／折り返しで異なる点（ドラッグ均等割り vs 明示的な列数設定UI）が
  新たに判明した差分。

**E. 基本エリア（単体エリア）の親要素は「編集画面限定」でdivに変換される、
公開時は何も追加出力しない**
`<ul class="navigation">{%%rdarea id="cmn001" name="ナビゲーション" %%}</ul>`のように
div以外を親にしても、**編集画面では親要素が強制的に`<div class="navigation
rdareaWrapper" ...>`のような形でタグ自体がdivに変換される**（クラスは維持される）。
一方、**公開されたサイト上では基本エリア（単体・1個だけの場合）は元の親タグ・元の
属性のまま、余計な要素は一切追加出力されない**ことが明記されている。この「公開時は
無害」という前提は、12節Iで発見した「本番出力でもラップdivが増えるケースがある」
という知見と**矛盾しない**：12節Iのケースは`<li>`を繰り返し配置する再帰エリア
（Hのナビゲーション構造）という特殊な"リスト型"の話であり、単一の基本エリアには
適用されない、という切り分けができた。

**F. ブロックの編集画面ラップは実際には「3重」（8節の記述を訂正）**
8節では`.rdareaItem`→`.rdareaContents`の**2重**ラップと説明していたが、公式資料
では明確に「ブロックファイルの外側に**3重**のdivが追加されます」とあり、実際には
`.rdareaItem`（属性: `id`/`data-areaid`/`data-areaname`/`data-rduni`/`data-iscmn`/
`data-blocks`/`class`にrdareaItem-first・rdareaItem-last等が付く/`style`＝ブロック
CSS設定時にここへ入る）→ `.rdareaContents`（属性なし、classのみ）→
**無属性の素のdiv（3階層目）** → 元のブロックHTML、という構成。既存の8節の図解を
この3重構成に訂正する必要がある。
また、**`.rdareaItem`が持つ`opacity`/`position`/`transform`/`transition-*`/`z-index`
は編集画面の選択・ドラッグ機能を維持するために重要なプロパティであり、テンプレート
側のCSSで（`.rdareaItem`セレクタを直接使うなどして）意図せず上書きしないよう注意**
という公式の警告がある。`taisei-site/`は現状`.rdareaItem`を直接ターゲットにした
CSSを書いていないため問題ないが、今後`#editView`向けのCSSを追加する際はこの制約を
踏まえること。

**G. テキストエディタ（CKEditor5ベース）の装飾・リンクの実際の出力形式**
- 文字サイズ：`<span class="text-fontsize1〜7" style="font-size:...">`
- フォント：`<span class="text-fontfamily-xxx" style="font-family:...">`
- 文字色・背景色：`<span style="color:...">`／`<span style="background-color:...">`
- 下線／太字／斜体：`<u>`／`<strong>`／`<i>`（3節の「`<strong>/<i>/<u>`を直接使う
  のが無難」という既存の推奨と完全に一致・裏付けされた）
- **サイト内リンク・ファイルリンクの実際の仕組み**：編集画面では
  `<a href="ページ名称" data-rdhref="{url@ページID}">`のように、hrefには表示用の
  ラベル文字列、`data-rdhref`に内部プレースホルダーが入る。**出力時に`data-rdhref`
  属性は削除され、hrefが実際のURLに書き換わる**。外部リンクは単純に
  `<a href="http://...">`（別ウインドウなら`target="_blank"`が付く）。
- **`<p>`の自動付与ルール**：テキストブロックの中身は基本的に前後に`<p></p>`が
  自動挿入されるが、**そのテキストブロックの祖先要素がh2〜h6の場合は`<p>`が入らない**
  （見出しブロック`.rdplanetitle`のように、`<h2 rdid="edtxt_1">{%%edtxt_1%%}</h2>`
  という構造ではpタグが付かない。ただし編集モード中だけ一時的にpが入る）。
- 改行は`<br>`に変換される（連続改行は連続`<br>`のまま維持）。

**H. 「JavaScriptは内容変更画面で実行されない」という8節の説明に精度上の補足**
`.rdslide`（画像スライドブロック）の資料に「**内容変更画面ではforce.jsを含めて
テンプレートHTMLで使用してる外部jsファイルは読み込まれません**」と明記されている
一方、「スライドショーは内容変更画面上でも実行されます」ともあり、**WebChanger
自身が提供する標準ブロック（スライドショー等）は、force.jsとは別に編集画面専用の
内製スクリプトで同等の動作を再現している**ことが分かった。つまり正確には：
**「テンプレート側が読み込む外部JSファイル（force.jsや`common/js/script.js`など）は
編集画面では一切実行されない」のが原則だが、WebChanger標準ブロックの一部は
編集画面向けに独自の代替実装を持っており、見た目上は動いて見える**、という整理が
正しい。`taisei-site/`の`.reveal`・ヒーロースライダー・ナビトグルはすべて独自実装
（標準ブロックではない）なので、8節の対処方針（`#editView`向けの強制表示CSS等）
は引き続きそのまま有効。

**I. ブロックリンク機能（`rdhaslink`）の実装詳細**
ブロック全体をリンク化する機能。出力時はブロック最上位に`rdhaslink`クラスが付与され、
閉じタグ直前に`<a href="リンク先" class="rdblocklink"></a>`が挿入される。CSSは
`.rdhaslink{position:relative}` `.rdhaslink>a.rdblocklink{position:absolute;
inset:0;width:100%;height:100%;z-index:1000}`という、要素全体に重なる透明アンカー
方式。**注意点**：この透明アンカーがz-index最上位に重なるため、リンク化された
ブロック内のテキストはドラッグ選択できず、画像も右クリック保存ができなくなる
（コンテンツ編集者への案内事項として、将来バナー的なブロックを作る際に周知が必要）。

**J. クラス追加機能の命名規則**
WebChanger管理画面の「クラス追加機能」で登録・入力できるクラス名は、
①アルファベットで始まる、②使える文字はアルファベット・数字・`-`・`_`のみ、
③**単語として`rd`で始まるクラス名は使用不可**（`rdclass`はNG、`xrdclass`や
途中に`rd`を含む`abcd-rd-01`はOK）というルールがある。これは
WebChangerシステム自身が使う`rd`プレフィックスの予約語空間との衝突を避けるため
と考えられる（`taisei-site/`側で今後カスタムブロックのクラスプリセット名を
提案する機会があれば、このルール厳守）。追加クラスはブロックの一番外側の要素に
付与される。

**K. 表示・非表示機能の内部仕様**
「表示・非表示を切り替える」機能で非表示にしたブロックは、**HTMLの出力から物理的に
削除される**（表示終了日時を過去に設定するという内部処理）。一方「PCでは非表示」
「スマホでは非表示」は`data-rdhide="pc"/"sp"`属性＋576pxブレークポイントの
メディアクエリで実現され、**HTML自体は常に出力される**（`taisei-site-プロジェクト情報.md`3.4節・本ガイド9節の既存記述と
完全に一致・裏付け済み）。

**L. 表ブロック（`.rdtable`）の編集キーと内部構造**
`<div class="rdtable rdblock"><div rdid="edtable_1">{%%edtable_1%%}</div></div>`。
出力後は`.rdtableTable`（テーブル本体）・`.rdthead`（ヘッダ行、無効時も常に出力され
`style="display:none"`になるだけ）・`.rdtbody`（本体、常に出力）という構成。
「スマホでの表示は横スクロールにする」オプション有効時のみ`.rdResponsive-table`
クラスが追加される。セルの中身はテキスト・画像・地図・Youtubeを内包できる
（各々そのブロックと同じ仕様）。9節の`.rdtable-pattern01〜04`（装飾パターン）と
併せて把握しておくこと。なお、これは**リッチテキスト内にCKEditorのテーブル機能で
表を挿入した場合**（`<figure class="table"><table><tbody>...`という別の構造になる）
とは別物なので混同しないこと。

**未着手（次回以降のアップロード分・または未読分で確認予定）**：
画像スライドの詳細な全属性、地図・Youtube・SNS埋め込み4種、ブログ関連11ブロックの
個別仕様、お問い合わせ表示枠、スクロールアニメーション（`data-rdaos`）の全属性、
ブロック見本、force.css全文（既存9節の裏付け用として再照合予定）。
